import {
  __commonJS
} from "./chunk-G3PMV62Z.js";

// node_modules/base64-js/index.js
var require_base64_js = __commonJS({
  "node_modules/base64-js/index.js"(exports) {
    "use strict";
    exports.byteLength = byteLength;
    exports.toByteArray = toByteArray;
    exports.fromByteArray = fromByteArray;
    var lookup = [];
    var revLookup = [];
    var Arr = typeof Uint8Array !== "undefined" ? Uint8Array : Array;
    var code = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
    for (i = 0, len = code.length; i < len; ++i) {
      lookup[i] = code[i];
      revLookup[code.charCodeAt(i)] = i;
    }
    var i;
    var len;
    revLookup["-".charCodeAt(0)] = 62;
    revLookup["_".charCodeAt(0)] = 63;
    function getLens(b64) {
      var len2 = b64.length;
      if (len2 % 4 > 0) {
        throw new Error("Invalid string. Length must be a multiple of 4");
      }
      var validLen = b64.indexOf("=");
      if (validLen === -1) validLen = len2;
      var placeHoldersLen = validLen === len2 ? 0 : 4 - validLen % 4;
      return [validLen, placeHoldersLen];
    }
    function byteLength(b64) {
      var lens = getLens(b64);
      var validLen = lens[0];
      var placeHoldersLen = lens[1];
      return (validLen + placeHoldersLen) * 3 / 4 - placeHoldersLen;
    }
    function _byteLength(b64, validLen, placeHoldersLen) {
      return (validLen + placeHoldersLen) * 3 / 4 - placeHoldersLen;
    }
    function toByteArray(b64) {
      var tmp;
      var lens = getLens(b64);
      var validLen = lens[0];
      var placeHoldersLen = lens[1];
      var arr = new Arr(_byteLength(b64, validLen, placeHoldersLen));
      var curByte = 0;
      var len2 = placeHoldersLen > 0 ? validLen - 4 : validLen;
      var i2;
      for (i2 = 0; i2 < len2; i2 += 4) {
        tmp = revLookup[b64.charCodeAt(i2)] << 18 | revLookup[b64.charCodeAt(i2 + 1)] << 12 | revLookup[b64.charCodeAt(i2 + 2)] << 6 | revLookup[b64.charCodeAt(i2 + 3)];
        arr[curByte++] = tmp >> 16 & 255;
        arr[curByte++] = tmp >> 8 & 255;
        arr[curByte++] = tmp & 255;
      }
      if (placeHoldersLen === 2) {
        tmp = revLookup[b64.charCodeAt(i2)] << 2 | revLookup[b64.charCodeAt(i2 + 1)] >> 4;
        arr[curByte++] = tmp & 255;
      }
      if (placeHoldersLen === 1) {
        tmp = revLookup[b64.charCodeAt(i2)] << 10 | revLookup[b64.charCodeAt(i2 + 1)] << 4 | revLookup[b64.charCodeAt(i2 + 2)] >> 2;
        arr[curByte++] = tmp >> 8 & 255;
        arr[curByte++] = tmp & 255;
      }
      return arr;
    }
    function tripletToBase64(num) {
      return lookup[num >> 18 & 63] + lookup[num >> 12 & 63] + lookup[num >> 6 & 63] + lookup[num & 63];
    }
    function encodeChunk(uint8, start, end) {
      var tmp;
      var output = [];
      for (var i2 = start; i2 < end; i2 += 3) {
        tmp = (uint8[i2] << 16 & 16711680) + (uint8[i2 + 1] << 8 & 65280) + (uint8[i2 + 2] & 255);
        output.push(tripletToBase64(tmp));
      }
      return output.join("");
    }
    function fromByteArray(uint8) {
      var tmp;
      var len2 = uint8.length;
      var extraBytes = len2 % 3;
      var parts = [];
      var maxChunkLength = 16383;
      for (var i2 = 0, len22 = len2 - extraBytes; i2 < len22; i2 += maxChunkLength) {
        parts.push(encodeChunk(uint8, i2, i2 + maxChunkLength > len22 ? len22 : i2 + maxChunkLength));
      }
      if (extraBytes === 1) {
        tmp = uint8[len2 - 1];
        parts.push(
          lookup[tmp >> 2] + lookup[tmp << 4 & 63] + "=="
        );
      } else if (extraBytes === 2) {
        tmp = (uint8[len2 - 2] << 8) + uint8[len2 - 1];
        parts.push(
          lookup[tmp >> 10] + lookup[tmp >> 4 & 63] + lookup[tmp << 2 & 63] + "="
        );
      }
      return parts.join("");
    }
  }
});

// node_modules/ieee754/index.js
var require_ieee754 = __commonJS({
  "node_modules/ieee754/index.js"(exports) {
    exports.read = function(buffer, offset, isLE, mLen, nBytes) {
      var e, m;
      var eLen = nBytes * 8 - mLen - 1;
      var eMax = (1 << eLen) - 1;
      var eBias = eMax >> 1;
      var nBits = -7;
      var i = isLE ? nBytes - 1 : 0;
      var d = isLE ? -1 : 1;
      var s = buffer[offset + i];
      i += d;
      e = s & (1 << -nBits) - 1;
      s >>= -nBits;
      nBits += eLen;
      for (; nBits > 0; e = e * 256 + buffer[offset + i], i += d, nBits -= 8) {
      }
      m = e & (1 << -nBits) - 1;
      e >>= -nBits;
      nBits += mLen;
      for (; nBits > 0; m = m * 256 + buffer[offset + i], i += d, nBits -= 8) {
      }
      if (e === 0) {
        e = 1 - eBias;
      } else if (e === eMax) {
        return m ? NaN : (s ? -1 : 1) * Infinity;
      } else {
        m = m + Math.pow(2, mLen);
        e = e - eBias;
      }
      return (s ? -1 : 1) * m * Math.pow(2, e - mLen);
    };
    exports.write = function(buffer, value, offset, isLE, mLen, nBytes) {
      var e, m, c;
      var eLen = nBytes * 8 - mLen - 1;
      var eMax = (1 << eLen) - 1;
      var eBias = eMax >> 1;
      var rt = mLen === 23 ? Math.pow(2, -24) - Math.pow(2, -77) : 0;
      var i = isLE ? 0 : nBytes - 1;
      var d = isLE ? 1 : -1;
      var s = value < 0 || value === 0 && 1 / value < 0 ? 1 : 0;
      value = Math.abs(value);
      if (isNaN(value) || value === Infinity) {
        m = isNaN(value) ? 1 : 0;
        e = eMax;
      } else {
        e = Math.floor(Math.log(value) / Math.LN2);
        if (value * (c = Math.pow(2, -e)) < 1) {
          e--;
          c *= 2;
        }
        if (e + eBias >= 1) {
          value += rt / c;
        } else {
          value += rt * Math.pow(2, 1 - eBias);
        }
        if (value * c >= 2) {
          e++;
          c /= 2;
        }
        if (e + eBias >= eMax) {
          m = 0;
          e = eMax;
        } else if (e + eBias >= 1) {
          m = (value * c - 1) * Math.pow(2, mLen);
          e = e + eBias;
        } else {
          m = value * Math.pow(2, eBias - 1) * Math.pow(2, mLen);
          e = 0;
        }
      }
      for (; mLen >= 8; buffer[offset + i] = m & 255, i += d, m /= 256, mLen -= 8) {
      }
      e = e << mLen | m;
      eLen += mLen;
      for (; eLen > 0; buffer[offset + i] = e & 255, i += d, e /= 256, eLen -= 8) {
      }
      buffer[offset + i - d] |= s * 128;
    };
  }
});

// node_modules/buffer/index.js
var require_buffer = __commonJS({
  "node_modules/buffer/index.js"(exports) {
    "use strict";
    var base64 = require_base64_js();
    var ieee754 = require_ieee754();
    var customInspectSymbol = typeof Symbol === "function" && typeof Symbol["for"] === "function" ? Symbol["for"]("nodejs.util.inspect.custom") : null;
    exports.Buffer = Buffer;
    exports.SlowBuffer = SlowBuffer;
    exports.INSPECT_MAX_BYTES = 50;
    var K_MAX_LENGTH = 2147483647;
    exports.kMaxLength = K_MAX_LENGTH;
    Buffer.TYPED_ARRAY_SUPPORT = typedArraySupport();
    if (!Buffer.TYPED_ARRAY_SUPPORT && typeof console !== "undefined" && typeof console.error === "function") {
      console.error(
        "This browser lacks typed array (Uint8Array) support which is required by `buffer` v5.x. Use `buffer` v4.x if you require old browser support."
      );
    }
    function typedArraySupport() {
      try {
        const arr = new Uint8Array(1);
        const proto = { foo: function() {
          return 42;
        } };
        Object.setPrototypeOf(proto, Uint8Array.prototype);
        Object.setPrototypeOf(arr, proto);
        return arr.foo() === 42;
      } catch (e) {
        return false;
      }
    }
    Object.defineProperty(Buffer.prototype, "parent", {
      enumerable: true,
      get: function() {
        if (!Buffer.isBuffer(this)) return void 0;
        return this.buffer;
      }
    });
    Object.defineProperty(Buffer.prototype, "offset", {
      enumerable: true,
      get: function() {
        if (!Buffer.isBuffer(this)) return void 0;
        return this.byteOffset;
      }
    });
    function createBuffer(length) {
      if (length > K_MAX_LENGTH) {
        throw new RangeError('The value "' + length + '" is invalid for option "size"');
      }
      const buf = new Uint8Array(length);
      Object.setPrototypeOf(buf, Buffer.prototype);
      return buf;
    }
    function Buffer(arg, encodingOrOffset, length) {
      if (typeof arg === "number") {
        if (typeof encodingOrOffset === "string") {
          throw new TypeError(
            'The "string" argument must be of type string. Received type number'
          );
        }
        return allocUnsafe(arg);
      }
      return from(arg, encodingOrOffset, length);
    }
    Buffer.poolSize = 8192;
    function from(value, encodingOrOffset, length) {
      if (typeof value === "string") {
        return fromString(value, encodingOrOffset);
      }
      if (ArrayBuffer.isView(value)) {
        return fromArrayView(value);
      }
      if (value == null) {
        throw new TypeError(
          "The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof value
        );
      }
      if (isInstance(value, ArrayBuffer) || value && isInstance(value.buffer, ArrayBuffer)) {
        return fromArrayBuffer(value, encodingOrOffset, length);
      }
      if (typeof SharedArrayBuffer !== "undefined" && (isInstance(value, SharedArrayBuffer) || value && isInstance(value.buffer, SharedArrayBuffer))) {
        return fromArrayBuffer(value, encodingOrOffset, length);
      }
      if (typeof value === "number") {
        throw new TypeError(
          'The "value" argument must not be of type number. Received type number'
        );
      }
      const valueOf = value.valueOf && value.valueOf();
      if (valueOf != null && valueOf !== value) {
        return Buffer.from(valueOf, encodingOrOffset, length);
      }
      const b = fromObject(value);
      if (b) return b;
      if (typeof Symbol !== "undefined" && Symbol.toPrimitive != null && typeof value[Symbol.toPrimitive] === "function") {
        return Buffer.from(value[Symbol.toPrimitive]("string"), encodingOrOffset, length);
      }
      throw new TypeError(
        "The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof value
      );
    }
    Buffer.from = function(value, encodingOrOffset, length) {
      return from(value, encodingOrOffset, length);
    };
    Object.setPrototypeOf(Buffer.prototype, Uint8Array.prototype);
    Object.setPrototypeOf(Buffer, Uint8Array);
    function assertSize(size) {
      if (typeof size !== "number") {
        throw new TypeError('"size" argument must be of type number');
      } else if (size < 0) {
        throw new RangeError('The value "' + size + '" is invalid for option "size"');
      }
    }
    function alloc(size, fill, encoding) {
      assertSize(size);
      if (size <= 0) {
        return createBuffer(size);
      }
      if (fill !== void 0) {
        return typeof encoding === "string" ? createBuffer(size).fill(fill, encoding) : createBuffer(size).fill(fill);
      }
      return createBuffer(size);
    }
    Buffer.alloc = function(size, fill, encoding) {
      return alloc(size, fill, encoding);
    };
    function allocUnsafe(size) {
      assertSize(size);
      return createBuffer(size < 0 ? 0 : checked(size) | 0);
    }
    Buffer.allocUnsafe = function(size) {
      return allocUnsafe(size);
    };
    Buffer.allocUnsafeSlow = function(size) {
      return allocUnsafe(size);
    };
    function fromString(string, encoding) {
      if (typeof encoding !== "string" || encoding === "") {
        encoding = "utf8";
      }
      if (!Buffer.isEncoding(encoding)) {
        throw new TypeError("Unknown encoding: " + encoding);
      }
      const length = byteLength(string, encoding) | 0;
      let buf = createBuffer(length);
      const actual = buf.write(string, encoding);
      if (actual !== length) {
        buf = buf.slice(0, actual);
      }
      return buf;
    }
    function fromArrayLike(array) {
      const length = array.length < 0 ? 0 : checked(array.length) | 0;
      const buf = createBuffer(length);
      for (let i = 0; i < length; i += 1) {
        buf[i] = array[i] & 255;
      }
      return buf;
    }
    function fromArrayView(arrayView) {
      if (isInstance(arrayView, Uint8Array)) {
        const copy = new Uint8Array(arrayView);
        return fromArrayBuffer(copy.buffer, copy.byteOffset, copy.byteLength);
      }
      return fromArrayLike(arrayView);
    }
    function fromArrayBuffer(array, byteOffset, length) {
      if (byteOffset < 0 || array.byteLength < byteOffset) {
        throw new RangeError('"offset" is outside of buffer bounds');
      }
      if (array.byteLength < byteOffset + (length || 0)) {
        throw new RangeError('"length" is outside of buffer bounds');
      }
      let buf;
      if (byteOffset === void 0 && length === void 0) {
        buf = new Uint8Array(array);
      } else if (length === void 0) {
        buf = new Uint8Array(array, byteOffset);
      } else {
        buf = new Uint8Array(array, byteOffset, length);
      }
      Object.setPrototypeOf(buf, Buffer.prototype);
      return buf;
    }
    function fromObject(obj) {
      if (Buffer.isBuffer(obj)) {
        const len = checked(obj.length) | 0;
        const buf = createBuffer(len);
        if (buf.length === 0) {
          return buf;
        }
        obj.copy(buf, 0, 0, len);
        return buf;
      }
      if (obj.length !== void 0) {
        if (typeof obj.length !== "number" || numberIsNaN(obj.length)) {
          return createBuffer(0);
        }
        return fromArrayLike(obj);
      }
      if (obj.type === "Buffer" && Array.isArray(obj.data)) {
        return fromArrayLike(obj.data);
      }
    }
    function checked(length) {
      if (length >= K_MAX_LENGTH) {
        throw new RangeError("Attempt to allocate Buffer larger than maximum size: 0x" + K_MAX_LENGTH.toString(16) + " bytes");
      }
      return length | 0;
    }
    function SlowBuffer(length) {
      if (+length != length) {
        length = 0;
      }
      return Buffer.alloc(+length);
    }
    Buffer.isBuffer = function isBuffer(b) {
      return b != null && b._isBuffer === true && b !== Buffer.prototype;
    };
    Buffer.compare = function compare(a, b) {
      if (isInstance(a, Uint8Array)) a = Buffer.from(a, a.offset, a.byteLength);
      if (isInstance(b, Uint8Array)) b = Buffer.from(b, b.offset, b.byteLength);
      if (!Buffer.isBuffer(a) || !Buffer.isBuffer(b)) {
        throw new TypeError(
          'The "buf1", "buf2" arguments must be one of type Buffer or Uint8Array'
        );
      }
      if (a === b) return 0;
      let x = a.length;
      let y = b.length;
      for (let i = 0, len = Math.min(x, y); i < len; ++i) {
        if (a[i] !== b[i]) {
          x = a[i];
          y = b[i];
          break;
        }
      }
      if (x < y) return -1;
      if (y < x) return 1;
      return 0;
    };
    Buffer.isEncoding = function isEncoding(encoding) {
      switch (String(encoding).toLowerCase()) {
        case "hex":
        case "utf8":
        case "utf-8":
        case "ascii":
        case "latin1":
        case "binary":
        case "base64":
        case "ucs2":
        case "ucs-2":
        case "utf16le":
        case "utf-16le":
          return true;
        default:
          return false;
      }
    };
    Buffer.concat = function concat(list, length) {
      if (!Array.isArray(list)) {
        throw new TypeError('"list" argument must be an Array of Buffers');
      }
      if (list.length === 0) {
        return Buffer.alloc(0);
      }
      let i;
      if (length === void 0) {
        length = 0;
        for (i = 0; i < list.length; ++i) {
          length += list[i].length;
        }
      }
      const buffer = Buffer.allocUnsafe(length);
      let pos = 0;
      for (i = 0; i < list.length; ++i) {
        let buf = list[i];
        if (isInstance(buf, Uint8Array)) {
          if (pos + buf.length > buffer.length) {
            if (!Buffer.isBuffer(buf)) buf = Buffer.from(buf);
            buf.copy(buffer, pos);
          } else {
            Uint8Array.prototype.set.call(
              buffer,
              buf,
              pos
            );
          }
        } else if (!Buffer.isBuffer(buf)) {
          throw new TypeError('"list" argument must be an Array of Buffers');
        } else {
          buf.copy(buffer, pos);
        }
        pos += buf.length;
      }
      return buffer;
    };
    function byteLength(string, encoding) {
      if (Buffer.isBuffer(string)) {
        return string.length;
      }
      if (ArrayBuffer.isView(string) || isInstance(string, ArrayBuffer)) {
        return string.byteLength;
      }
      if (typeof string !== "string") {
        throw new TypeError(
          'The "string" argument must be one of type string, Buffer, or ArrayBuffer. Received type ' + typeof string
        );
      }
      const len = string.length;
      const mustMatch = arguments.length > 2 && arguments[2] === true;
      if (!mustMatch && len === 0) return 0;
      let loweredCase = false;
      for (; ; ) {
        switch (encoding) {
          case "ascii":
          case "latin1":
          case "binary":
            return len;
          case "utf8":
          case "utf-8":
            return utf8ToBytes(string).length;
          case "ucs2":
          case "ucs-2":
          case "utf16le":
          case "utf-16le":
            return len * 2;
          case "hex":
            return len >>> 1;
          case "base64":
            return base64ToBytes(string).length;
          default:
            if (loweredCase) {
              return mustMatch ? -1 : utf8ToBytes(string).length;
            }
            encoding = ("" + encoding).toLowerCase();
            loweredCase = true;
        }
      }
    }
    Buffer.byteLength = byteLength;
    function slowToString(encoding, start, end) {
      let loweredCase = false;
      if (start === void 0 || start < 0) {
        start = 0;
      }
      if (start > this.length) {
        return "";
      }
      if (end === void 0 || end > this.length) {
        end = this.length;
      }
      if (end <= 0) {
        return "";
      }
      end >>>= 0;
      start >>>= 0;
      if (end <= start) {
        return "";
      }
      if (!encoding) encoding = "utf8";
      while (true) {
        switch (encoding) {
          case "hex":
            return hexSlice(this, start, end);
          case "utf8":
          case "utf-8":
            return utf8Slice(this, start, end);
          case "ascii":
            return asciiSlice(this, start, end);
          case "latin1":
          case "binary":
            return latin1Slice(this, start, end);
          case "base64":
            return base64Slice(this, start, end);
          case "ucs2":
          case "ucs-2":
          case "utf16le":
          case "utf-16le":
            return utf16leSlice(this, start, end);
          default:
            if (loweredCase) throw new TypeError("Unknown encoding: " + encoding);
            encoding = (encoding + "").toLowerCase();
            loweredCase = true;
        }
      }
    }
    Buffer.prototype._isBuffer = true;
    function swap(b, n, m) {
      const i = b[n];
      b[n] = b[m];
      b[m] = i;
    }
    Buffer.prototype.swap16 = function swap16() {
      const len = this.length;
      if (len % 2 !== 0) {
        throw new RangeError("Buffer size must be a multiple of 16-bits");
      }
      for (let i = 0; i < len; i += 2) {
        swap(this, i, i + 1);
      }
      return this;
    };
    Buffer.prototype.swap32 = function swap32() {
      const len = this.length;
      if (len % 4 !== 0) {
        throw new RangeError("Buffer size must be a multiple of 32-bits");
      }
      for (let i = 0; i < len; i += 4) {
        swap(this, i, i + 3);
        swap(this, i + 1, i + 2);
      }
      return this;
    };
    Buffer.prototype.swap64 = function swap64() {
      const len = this.length;
      if (len % 8 !== 0) {
        throw new RangeError("Buffer size must be a multiple of 64-bits");
      }
      for (let i = 0; i < len; i += 8) {
        swap(this, i, i + 7);
        swap(this, i + 1, i + 6);
        swap(this, i + 2, i + 5);
        swap(this, i + 3, i + 4);
      }
      return this;
    };
    Buffer.prototype.toString = function toString() {
      const length = this.length;
      if (length === 0) return "";
      if (arguments.length === 0) return utf8Slice(this, 0, length);
      return slowToString.apply(this, arguments);
    };
    Buffer.prototype.toLocaleString = Buffer.prototype.toString;
    Buffer.prototype.equals = function equals(b) {
      if (!Buffer.isBuffer(b)) throw new TypeError("Argument must be a Buffer");
      if (this === b) return true;
      return Buffer.compare(this, b) === 0;
    };
    Buffer.prototype.inspect = function inspect() {
      let str = "";
      const max = exports.INSPECT_MAX_BYTES;
      str = this.toString("hex", 0, max).replace(/(.{2})/g, "$1 ").trim();
      if (this.length > max) str += " ... ";
      return "<Buffer " + str + ">";
    };
    if (customInspectSymbol) {
      Buffer.prototype[customInspectSymbol] = Buffer.prototype.inspect;
    }
    Buffer.prototype.compare = function compare(target, start, end, thisStart, thisEnd) {
      if (isInstance(target, Uint8Array)) {
        target = Buffer.from(target, target.offset, target.byteLength);
      }
      if (!Buffer.isBuffer(target)) {
        throw new TypeError(
          'The "target" argument must be one of type Buffer or Uint8Array. Received type ' + typeof target
        );
      }
      if (start === void 0) {
        start = 0;
      }
      if (end === void 0) {
        end = target ? target.length : 0;
      }
      if (thisStart === void 0) {
        thisStart = 0;
      }
      if (thisEnd === void 0) {
        thisEnd = this.length;
      }
      if (start < 0 || end > target.length || thisStart < 0 || thisEnd > this.length) {
        throw new RangeError("out of range index");
      }
      if (thisStart >= thisEnd && start >= end) {
        return 0;
      }
      if (thisStart >= thisEnd) {
        return -1;
      }
      if (start >= end) {
        return 1;
      }
      start >>>= 0;
      end >>>= 0;
      thisStart >>>= 0;
      thisEnd >>>= 0;
      if (this === target) return 0;
      let x = thisEnd - thisStart;
      let y = end - start;
      const len = Math.min(x, y);
      const thisCopy = this.slice(thisStart, thisEnd);
      const targetCopy = target.slice(start, end);
      for (let i = 0; i < len; ++i) {
        if (thisCopy[i] !== targetCopy[i]) {
          x = thisCopy[i];
          y = targetCopy[i];
          break;
        }
      }
      if (x < y) return -1;
      if (y < x) return 1;
      return 0;
    };
    function bidirectionalIndexOf(buffer, val, byteOffset, encoding, dir) {
      if (buffer.length === 0) return -1;
      if (typeof byteOffset === "string") {
        encoding = byteOffset;
        byteOffset = 0;
      } else if (byteOffset > 2147483647) {
        byteOffset = 2147483647;
      } else if (byteOffset < -2147483648) {
        byteOffset = -2147483648;
      }
      byteOffset = +byteOffset;
      if (numberIsNaN(byteOffset)) {
        byteOffset = dir ? 0 : buffer.length - 1;
      }
      if (byteOffset < 0) byteOffset = buffer.length + byteOffset;
      if (byteOffset >= buffer.length) {
        if (dir) return -1;
        else byteOffset = buffer.length - 1;
      } else if (byteOffset < 0) {
        if (dir) byteOffset = 0;
        else return -1;
      }
      if (typeof val === "string") {
        val = Buffer.from(val, encoding);
      }
      if (Buffer.isBuffer(val)) {
        if (val.length === 0) {
          return -1;
        }
        return arrayIndexOf(buffer, val, byteOffset, encoding, dir);
      } else if (typeof val === "number") {
        val = val & 255;
        if (typeof Uint8Array.prototype.indexOf === "function") {
          if (dir) {
            return Uint8Array.prototype.indexOf.call(buffer, val, byteOffset);
          } else {
            return Uint8Array.prototype.lastIndexOf.call(buffer, val, byteOffset);
          }
        }
        return arrayIndexOf(buffer, [val], byteOffset, encoding, dir);
      }
      throw new TypeError("val must be string, number or Buffer");
    }
    function arrayIndexOf(arr, val, byteOffset, encoding, dir) {
      let indexSize = 1;
      let arrLength = arr.length;
      let valLength = val.length;
      if (encoding !== void 0) {
        encoding = String(encoding).toLowerCase();
        if (encoding === "ucs2" || encoding === "ucs-2" || encoding === "utf16le" || encoding === "utf-16le") {
          if (arr.length < 2 || val.length < 2) {
            return -1;
          }
          indexSize = 2;
          arrLength /= 2;
          valLength /= 2;
          byteOffset /= 2;
        }
      }
      function read(buf, i2) {
        if (indexSize === 1) {
          return buf[i2];
        } else {
          return buf.readUInt16BE(i2 * indexSize);
        }
      }
      let i;
      if (dir) {
        let foundIndex = -1;
        for (i = byteOffset; i < arrLength; i++) {
          if (read(arr, i) === read(val, foundIndex === -1 ? 0 : i - foundIndex)) {
            if (foundIndex === -1) foundIndex = i;
            if (i - foundIndex + 1 === valLength) return foundIndex * indexSize;
          } else {
            if (foundIndex !== -1) i -= i - foundIndex;
            foundIndex = -1;
          }
        }
      } else {
        if (byteOffset + valLength > arrLength) byteOffset = arrLength - valLength;
        for (i = byteOffset; i >= 0; i--) {
          let found = true;
          for (let j = 0; j < valLength; j++) {
            if (read(arr, i + j) !== read(val, j)) {
              found = false;
              break;
            }
          }
          if (found) return i;
        }
      }
      return -1;
    }
    Buffer.prototype.includes = function includes(val, byteOffset, encoding) {
      return this.indexOf(val, byteOffset, encoding) !== -1;
    };
    Buffer.prototype.indexOf = function indexOf(val, byteOffset, encoding) {
      return bidirectionalIndexOf(this, val, byteOffset, encoding, true);
    };
    Buffer.prototype.lastIndexOf = function lastIndexOf(val, byteOffset, encoding) {
      return bidirectionalIndexOf(this, val, byteOffset, encoding, false);
    };
    function hexWrite(buf, string, offset, length) {
      offset = Number(offset) || 0;
      const remaining = buf.length - offset;
      if (!length) {
        length = remaining;
      } else {
        length = Number(length);
        if (length > remaining) {
          length = remaining;
        }
      }
      const strLen = string.length;
      if (length > strLen / 2) {
        length = strLen / 2;
      }
      let i;
      for (i = 0; i < length; ++i) {
        const parsed = parseInt(string.substr(i * 2, 2), 16);
        if (numberIsNaN(parsed)) return i;
        buf[offset + i] = parsed;
      }
      return i;
    }
    function utf8Write(buf, string, offset, length) {
      return blitBuffer(utf8ToBytes(string, buf.length - offset), buf, offset, length);
    }
    function asciiWrite(buf, string, offset, length) {
      return blitBuffer(asciiToBytes(string), buf, offset, length);
    }
    function base64Write(buf, string, offset, length) {
      return blitBuffer(base64ToBytes(string), buf, offset, length);
    }
    function ucs2Write(buf, string, offset, length) {
      return blitBuffer(utf16leToBytes(string, buf.length - offset), buf, offset, length);
    }
    Buffer.prototype.write = function write(string, offset, length, encoding) {
      if (offset === void 0) {
        encoding = "utf8";
        length = this.length;
        offset = 0;
      } else if (length === void 0 && typeof offset === "string") {
        encoding = offset;
        length = this.length;
        offset = 0;
      } else if (isFinite(offset)) {
        offset = offset >>> 0;
        if (isFinite(length)) {
          length = length >>> 0;
          if (encoding === void 0) encoding = "utf8";
        } else {
          encoding = length;
          length = void 0;
        }
      } else {
        throw new Error(
          "Buffer.write(string, encoding, offset[, length]) is no longer supported"
        );
      }
      const remaining = this.length - offset;
      if (length === void 0 || length > remaining) length = remaining;
      if (string.length > 0 && (length < 0 || offset < 0) || offset > this.length) {
        throw new RangeError("Attempt to write outside buffer bounds");
      }
      if (!encoding) encoding = "utf8";
      let loweredCase = false;
      for (; ; ) {
        switch (encoding) {
          case "hex":
            return hexWrite(this, string, offset, length);
          case "utf8":
          case "utf-8":
            return utf8Write(this, string, offset, length);
          case "ascii":
          case "latin1":
          case "binary":
            return asciiWrite(this, string, offset, length);
          case "base64":
            return base64Write(this, string, offset, length);
          case "ucs2":
          case "ucs-2":
          case "utf16le":
          case "utf-16le":
            return ucs2Write(this, string, offset, length);
          default:
            if (loweredCase) throw new TypeError("Unknown encoding: " + encoding);
            encoding = ("" + encoding).toLowerCase();
            loweredCase = true;
        }
      }
    };
    Buffer.prototype.toJSON = function toJSON() {
      return {
        type: "Buffer",
        data: Array.prototype.slice.call(this._arr || this, 0)
      };
    };
    function base64Slice(buf, start, end) {
      if (start === 0 && end === buf.length) {
        return base64.fromByteArray(buf);
      } else {
        return base64.fromByteArray(buf.slice(start, end));
      }
    }
    function utf8Slice(buf, start, end) {
      end = Math.min(buf.length, end);
      const res = [];
      let i = start;
      while (i < end) {
        const firstByte = buf[i];
        let codePoint = null;
        let bytesPerSequence = firstByte > 239 ? 4 : firstByte > 223 ? 3 : firstByte > 191 ? 2 : 1;
        if (i + bytesPerSequence <= end) {
          let secondByte, thirdByte, fourthByte, tempCodePoint;
          switch (bytesPerSequence) {
            case 1:
              if (firstByte < 128) {
                codePoint = firstByte;
              }
              break;
            case 2:
              secondByte = buf[i + 1];
              if ((secondByte & 192) === 128) {
                tempCodePoint = (firstByte & 31) << 6 | secondByte & 63;
                if (tempCodePoint > 127) {
                  codePoint = tempCodePoint;
                }
              }
              break;
            case 3:
              secondByte = buf[i + 1];
              thirdByte = buf[i + 2];
              if ((secondByte & 192) === 128 && (thirdByte & 192) === 128) {
                tempCodePoint = (firstByte & 15) << 12 | (secondByte & 63) << 6 | thirdByte & 63;
                if (tempCodePoint > 2047 && (tempCodePoint < 55296 || tempCodePoint > 57343)) {
                  codePoint = tempCodePoint;
                }
              }
              break;
            case 4:
              secondByte = buf[i + 1];
              thirdByte = buf[i + 2];
              fourthByte = buf[i + 3];
              if ((secondByte & 192) === 128 && (thirdByte & 192) === 128 && (fourthByte & 192) === 128) {
                tempCodePoint = (firstByte & 15) << 18 | (secondByte & 63) << 12 | (thirdByte & 63) << 6 | fourthByte & 63;
                if (tempCodePoint > 65535 && tempCodePoint < 1114112) {
                  codePoint = tempCodePoint;
                }
              }
          }
        }
        if (codePoint === null) {
          codePoint = 65533;
          bytesPerSequence = 1;
        } else if (codePoint > 65535) {
          codePoint -= 65536;
          res.push(codePoint >>> 10 & 1023 | 55296);
          codePoint = 56320 | codePoint & 1023;
        }
        res.push(codePoint);
        i += bytesPerSequence;
      }
      return decodeCodePointsArray(res);
    }
    var MAX_ARGUMENTS_LENGTH = 4096;
    function decodeCodePointsArray(codePoints) {
      const len = codePoints.length;
      if (len <= MAX_ARGUMENTS_LENGTH) {
        return String.fromCharCode.apply(String, codePoints);
      }
      let res = "";
      let i = 0;
      while (i < len) {
        res += String.fromCharCode.apply(
          String,
          codePoints.slice(i, i += MAX_ARGUMENTS_LENGTH)
        );
      }
      return res;
    }
    function asciiSlice(buf, start, end) {
      let ret = "";
      end = Math.min(buf.length, end);
      for (let i = start; i < end; ++i) {
        ret += String.fromCharCode(buf[i] & 127);
      }
      return ret;
    }
    function latin1Slice(buf, start, end) {
      let ret = "";
      end = Math.min(buf.length, end);
      for (let i = start; i < end; ++i) {
        ret += String.fromCharCode(buf[i]);
      }
      return ret;
    }
    function hexSlice(buf, start, end) {
      const len = buf.length;
      if (!start || start < 0) start = 0;
      if (!end || end < 0 || end > len) end = len;
      let out = "";
      for (let i = start; i < end; ++i) {
        out += hexSliceLookupTable[buf[i]];
      }
      return out;
    }
    function utf16leSlice(buf, start, end) {
      const bytes = buf.slice(start, end);
      let res = "";
      for (let i = 0; i < bytes.length - 1; i += 2) {
        res += String.fromCharCode(bytes[i] + bytes[i + 1] * 256);
      }
      return res;
    }
    Buffer.prototype.slice = function slice(start, end) {
      const len = this.length;
      start = ~~start;
      end = end === void 0 ? len : ~~end;
      if (start < 0) {
        start += len;
        if (start < 0) start = 0;
      } else if (start > len) {
        start = len;
      }
      if (end < 0) {
        end += len;
        if (end < 0) end = 0;
      } else if (end > len) {
        end = len;
      }
      if (end < start) end = start;
      const newBuf = this.subarray(start, end);
      Object.setPrototypeOf(newBuf, Buffer.prototype);
      return newBuf;
    };
    function checkOffset(offset, ext, length) {
      if (offset % 1 !== 0 || offset < 0) throw new RangeError("offset is not uint");
      if (offset + ext > length) throw new RangeError("Trying to access beyond buffer length");
    }
    Buffer.prototype.readUintLE = Buffer.prototype.readUIntLE = function readUIntLE(offset, byteLength2, noAssert) {
      offset = offset >>> 0;
      byteLength2 = byteLength2 >>> 0;
      if (!noAssert) checkOffset(offset, byteLength2, this.length);
      let val = this[offset];
      let mul = 1;
      let i = 0;
      while (++i < byteLength2 && (mul *= 256)) {
        val += this[offset + i] * mul;
      }
      return val;
    };
    Buffer.prototype.readUintBE = Buffer.prototype.readUIntBE = function readUIntBE(offset, byteLength2, noAssert) {
      offset = offset >>> 0;
      byteLength2 = byteLength2 >>> 0;
      if (!noAssert) {
        checkOffset(offset, byteLength2, this.length);
      }
      let val = this[offset + --byteLength2];
      let mul = 1;
      while (byteLength2 > 0 && (mul *= 256)) {
        val += this[offset + --byteLength2] * mul;
      }
      return val;
    };
    Buffer.prototype.readUint8 = Buffer.prototype.readUInt8 = function readUInt8(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 1, this.length);
      return this[offset];
    };
    Buffer.prototype.readUint16LE = Buffer.prototype.readUInt16LE = function readUInt16LE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 2, this.length);
      return this[offset] | this[offset + 1] << 8;
    };
    Buffer.prototype.readUint16BE = Buffer.prototype.readUInt16BE = function readUInt16BE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 2, this.length);
      return this[offset] << 8 | this[offset + 1];
    };
    Buffer.prototype.readUint32LE = Buffer.prototype.readUInt32LE = function readUInt32LE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 4, this.length);
      return (this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16) + this[offset + 3] * 16777216;
    };
    Buffer.prototype.readUint32BE = Buffer.prototype.readUInt32BE = function readUInt32BE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 4, this.length);
      return this[offset] * 16777216 + (this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3]);
    };
    Buffer.prototype.readBigUInt64LE = defineBigIntMethod(function readBigUInt64LE(offset) {
      offset = offset >>> 0;
      validateNumber(offset, "offset");
      const first = this[offset];
      const last = this[offset + 7];
      if (first === void 0 || last === void 0) {
        boundsError(offset, this.length - 8);
      }
      const lo = first + this[++offset] * 2 ** 8 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 24;
      const hi = this[++offset] + this[++offset] * 2 ** 8 + this[++offset] * 2 ** 16 + last * 2 ** 24;
      return BigInt(lo) + (BigInt(hi) << BigInt(32));
    });
    Buffer.prototype.readBigUInt64BE = defineBigIntMethod(function readBigUInt64BE(offset) {
      offset = offset >>> 0;
      validateNumber(offset, "offset");
      const first = this[offset];
      const last = this[offset + 7];
      if (first === void 0 || last === void 0) {
        boundsError(offset, this.length - 8);
      }
      const hi = first * 2 ** 24 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + this[++offset];
      const lo = this[++offset] * 2 ** 24 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + last;
      return (BigInt(hi) << BigInt(32)) + BigInt(lo);
    });
    Buffer.prototype.readIntLE = function readIntLE(offset, byteLength2, noAssert) {
      offset = offset >>> 0;
      byteLength2 = byteLength2 >>> 0;
      if (!noAssert) checkOffset(offset, byteLength2, this.length);
      let val = this[offset];
      let mul = 1;
      let i = 0;
      while (++i < byteLength2 && (mul *= 256)) {
        val += this[offset + i] * mul;
      }
      mul *= 128;
      if (val >= mul) val -= Math.pow(2, 8 * byteLength2);
      return val;
    };
    Buffer.prototype.readIntBE = function readIntBE(offset, byteLength2, noAssert) {
      offset = offset >>> 0;
      byteLength2 = byteLength2 >>> 0;
      if (!noAssert) checkOffset(offset, byteLength2, this.length);
      let i = byteLength2;
      let mul = 1;
      let val = this[offset + --i];
      while (i > 0 && (mul *= 256)) {
        val += this[offset + --i] * mul;
      }
      mul *= 128;
      if (val >= mul) val -= Math.pow(2, 8 * byteLength2);
      return val;
    };
    Buffer.prototype.readInt8 = function readInt8(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 1, this.length);
      if (!(this[offset] & 128)) return this[offset];
      return (255 - this[offset] + 1) * -1;
    };
    Buffer.prototype.readInt16LE = function readInt16LE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 2, this.length);
      const val = this[offset] | this[offset + 1] << 8;
      return val & 32768 ? val | 4294901760 : val;
    };
    Buffer.prototype.readInt16BE = function readInt16BE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 2, this.length);
      const val = this[offset + 1] | this[offset] << 8;
      return val & 32768 ? val | 4294901760 : val;
    };
    Buffer.prototype.readInt32LE = function readInt32LE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 4, this.length);
      return this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16 | this[offset + 3] << 24;
    };
    Buffer.prototype.readInt32BE = function readInt32BE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 4, this.length);
      return this[offset] << 24 | this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3];
    };
    Buffer.prototype.readBigInt64LE = defineBigIntMethod(function readBigInt64LE(offset) {
      offset = offset >>> 0;
      validateNumber(offset, "offset");
      const first = this[offset];
      const last = this[offset + 7];
      if (first === void 0 || last === void 0) {
        boundsError(offset, this.length - 8);
      }
      const val = this[offset + 4] + this[offset + 5] * 2 ** 8 + this[offset + 6] * 2 ** 16 + (last << 24);
      return (BigInt(val) << BigInt(32)) + BigInt(first + this[++offset] * 2 ** 8 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 24);
    });
    Buffer.prototype.readBigInt64BE = defineBigIntMethod(function readBigInt64BE(offset) {
      offset = offset >>> 0;
      validateNumber(offset, "offset");
      const first = this[offset];
      const last = this[offset + 7];
      if (first === void 0 || last === void 0) {
        boundsError(offset, this.length - 8);
      }
      const val = (first << 24) + // Overflow
      this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + this[++offset];
      return (BigInt(val) << BigInt(32)) + BigInt(this[++offset] * 2 ** 24 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + last);
    });
    Buffer.prototype.readFloatLE = function readFloatLE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 4, this.length);
      return ieee754.read(this, offset, true, 23, 4);
    };
    Buffer.prototype.readFloatBE = function readFloatBE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 4, this.length);
      return ieee754.read(this, offset, false, 23, 4);
    };
    Buffer.prototype.readDoubleLE = function readDoubleLE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 8, this.length);
      return ieee754.read(this, offset, true, 52, 8);
    };
    Buffer.prototype.readDoubleBE = function readDoubleBE(offset, noAssert) {
      offset = offset >>> 0;
      if (!noAssert) checkOffset(offset, 8, this.length);
      return ieee754.read(this, offset, false, 52, 8);
    };
    function checkInt(buf, value, offset, ext, max, min) {
      if (!Buffer.isBuffer(buf)) throw new TypeError('"buffer" argument must be a Buffer instance');
      if (value > max || value < min) throw new RangeError('"value" argument is out of bounds');
      if (offset + ext > buf.length) throw new RangeError("Index out of range");
    }
    Buffer.prototype.writeUintLE = Buffer.prototype.writeUIntLE = function writeUIntLE(value, offset, byteLength2, noAssert) {
      value = +value;
      offset = offset >>> 0;
      byteLength2 = byteLength2 >>> 0;
      if (!noAssert) {
        const maxBytes = Math.pow(2, 8 * byteLength2) - 1;
        checkInt(this, value, offset, byteLength2, maxBytes, 0);
      }
      let mul = 1;
      let i = 0;
      this[offset] = value & 255;
      while (++i < byteLength2 && (mul *= 256)) {
        this[offset + i] = value / mul & 255;
      }
      return offset + byteLength2;
    };
    Buffer.prototype.writeUintBE = Buffer.prototype.writeUIntBE = function writeUIntBE(value, offset, byteLength2, noAssert) {
      value = +value;
      offset = offset >>> 0;
      byteLength2 = byteLength2 >>> 0;
      if (!noAssert) {
        const maxBytes = Math.pow(2, 8 * byteLength2) - 1;
        checkInt(this, value, offset, byteLength2, maxBytes, 0);
      }
      let i = byteLength2 - 1;
      let mul = 1;
      this[offset + i] = value & 255;
      while (--i >= 0 && (mul *= 256)) {
        this[offset + i] = value / mul & 255;
      }
      return offset + byteLength2;
    };
    Buffer.prototype.writeUint8 = Buffer.prototype.writeUInt8 = function writeUInt8(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 1, 255, 0);
      this[offset] = value & 255;
      return offset + 1;
    };
    Buffer.prototype.writeUint16LE = Buffer.prototype.writeUInt16LE = function writeUInt16LE(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 2, 65535, 0);
      this[offset] = value & 255;
      this[offset + 1] = value >>> 8;
      return offset + 2;
    };
    Buffer.prototype.writeUint16BE = Buffer.prototype.writeUInt16BE = function writeUInt16BE(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 2, 65535, 0);
      this[offset] = value >>> 8;
      this[offset + 1] = value & 255;
      return offset + 2;
    };
    Buffer.prototype.writeUint32LE = Buffer.prototype.writeUInt32LE = function writeUInt32LE(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 4, 4294967295, 0);
      this[offset + 3] = value >>> 24;
      this[offset + 2] = value >>> 16;
      this[offset + 1] = value >>> 8;
      this[offset] = value & 255;
      return offset + 4;
    };
    Buffer.prototype.writeUint32BE = Buffer.prototype.writeUInt32BE = function writeUInt32BE(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 4, 4294967295, 0);
      this[offset] = value >>> 24;
      this[offset + 1] = value >>> 16;
      this[offset + 2] = value >>> 8;
      this[offset + 3] = value & 255;
      return offset + 4;
    };
    function wrtBigUInt64LE(buf, value, offset, min, max) {
      checkIntBI(value, min, max, buf, offset, 7);
      let lo = Number(value & BigInt(4294967295));
      buf[offset++] = lo;
      lo = lo >> 8;
      buf[offset++] = lo;
      lo = lo >> 8;
      buf[offset++] = lo;
      lo = lo >> 8;
      buf[offset++] = lo;
      let hi = Number(value >> BigInt(32) & BigInt(4294967295));
      buf[offset++] = hi;
      hi = hi >> 8;
      buf[offset++] = hi;
      hi = hi >> 8;
      buf[offset++] = hi;
      hi = hi >> 8;
      buf[offset++] = hi;
      return offset;
    }
    function wrtBigUInt64BE(buf, value, offset, min, max) {
      checkIntBI(value, min, max, buf, offset, 7);
      let lo = Number(value & BigInt(4294967295));
      buf[offset + 7] = lo;
      lo = lo >> 8;
      buf[offset + 6] = lo;
      lo = lo >> 8;
      buf[offset + 5] = lo;
      lo = lo >> 8;
      buf[offset + 4] = lo;
      let hi = Number(value >> BigInt(32) & BigInt(4294967295));
      buf[offset + 3] = hi;
      hi = hi >> 8;
      buf[offset + 2] = hi;
      hi = hi >> 8;
      buf[offset + 1] = hi;
      hi = hi >> 8;
      buf[offset] = hi;
      return offset + 8;
    }
    Buffer.prototype.writeBigUInt64LE = defineBigIntMethod(function writeBigUInt64LE(value, offset = 0) {
      return wrtBigUInt64LE(this, value, offset, BigInt(0), BigInt("0xffffffffffffffff"));
    });
    Buffer.prototype.writeBigUInt64BE = defineBigIntMethod(function writeBigUInt64BE(value, offset = 0) {
      return wrtBigUInt64BE(this, value, offset, BigInt(0), BigInt("0xffffffffffffffff"));
    });
    Buffer.prototype.writeIntLE = function writeIntLE(value, offset, byteLength2, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) {
        const limit = Math.pow(2, 8 * byteLength2 - 1);
        checkInt(this, value, offset, byteLength2, limit - 1, -limit);
      }
      let i = 0;
      let mul = 1;
      let sub = 0;
      this[offset] = value & 255;
      while (++i < byteLength2 && (mul *= 256)) {
        if (value < 0 && sub === 0 && this[offset + i - 1] !== 0) {
          sub = 1;
        }
        this[offset + i] = (value / mul >> 0) - sub & 255;
      }
      return offset + byteLength2;
    };
    Buffer.prototype.writeIntBE = function writeIntBE(value, offset, byteLength2, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) {
        const limit = Math.pow(2, 8 * byteLength2 - 1);
        checkInt(this, value, offset, byteLength2, limit - 1, -limit);
      }
      let i = byteLength2 - 1;
      let mul = 1;
      let sub = 0;
      this[offset + i] = value & 255;
      while (--i >= 0 && (mul *= 256)) {
        if (value < 0 && sub === 0 && this[offset + i + 1] !== 0) {
          sub = 1;
        }
        this[offset + i] = (value / mul >> 0) - sub & 255;
      }
      return offset + byteLength2;
    };
    Buffer.prototype.writeInt8 = function writeInt8(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 1, 127, -128);
      if (value < 0) value = 255 + value + 1;
      this[offset] = value & 255;
      return offset + 1;
    };
    Buffer.prototype.writeInt16LE = function writeInt16LE(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 2, 32767, -32768);
      this[offset] = value & 255;
      this[offset + 1] = value >>> 8;
      return offset + 2;
    };
    Buffer.prototype.writeInt16BE = function writeInt16BE(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 2, 32767, -32768);
      this[offset] = value >>> 8;
      this[offset + 1] = value & 255;
      return offset + 2;
    };
    Buffer.prototype.writeInt32LE = function writeInt32LE(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 4, 2147483647, -2147483648);
      this[offset] = value & 255;
      this[offset + 1] = value >>> 8;
      this[offset + 2] = value >>> 16;
      this[offset + 3] = value >>> 24;
      return offset + 4;
    };
    Buffer.prototype.writeInt32BE = function writeInt32BE(value, offset, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) checkInt(this, value, offset, 4, 2147483647, -2147483648);
      if (value < 0) value = 4294967295 + value + 1;
      this[offset] = value >>> 24;
      this[offset + 1] = value >>> 16;
      this[offset + 2] = value >>> 8;
      this[offset + 3] = value & 255;
      return offset + 4;
    };
    Buffer.prototype.writeBigInt64LE = defineBigIntMethod(function writeBigInt64LE(value, offset = 0) {
      return wrtBigUInt64LE(this, value, offset, -BigInt("0x8000000000000000"), BigInt("0x7fffffffffffffff"));
    });
    Buffer.prototype.writeBigInt64BE = defineBigIntMethod(function writeBigInt64BE(value, offset = 0) {
      return wrtBigUInt64BE(this, value, offset, -BigInt("0x8000000000000000"), BigInt("0x7fffffffffffffff"));
    });
    function checkIEEE754(buf, value, offset, ext, max, min) {
      if (offset + ext > buf.length) throw new RangeError("Index out of range");
      if (offset < 0) throw new RangeError("Index out of range");
    }
    function writeFloat(buf, value, offset, littleEndian, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) {
        checkIEEE754(buf, value, offset, 4, 34028234663852886e22, -34028234663852886e22);
      }
      ieee754.write(buf, value, offset, littleEndian, 23, 4);
      return offset + 4;
    }
    Buffer.prototype.writeFloatLE = function writeFloatLE(value, offset, noAssert) {
      return writeFloat(this, value, offset, true, noAssert);
    };
    Buffer.prototype.writeFloatBE = function writeFloatBE(value, offset, noAssert) {
      return writeFloat(this, value, offset, false, noAssert);
    };
    function writeDouble(buf, value, offset, littleEndian, noAssert) {
      value = +value;
      offset = offset >>> 0;
      if (!noAssert) {
        checkIEEE754(buf, value, offset, 8, 17976931348623157e292, -17976931348623157e292);
      }
      ieee754.write(buf, value, offset, littleEndian, 52, 8);
      return offset + 8;
    }
    Buffer.prototype.writeDoubleLE = function writeDoubleLE(value, offset, noAssert) {
      return writeDouble(this, value, offset, true, noAssert);
    };
    Buffer.prototype.writeDoubleBE = function writeDoubleBE(value, offset, noAssert) {
      return writeDouble(this, value, offset, false, noAssert);
    };
    Buffer.prototype.copy = function copy(target, targetStart, start, end) {
      if (!Buffer.isBuffer(target)) throw new TypeError("argument should be a Buffer");
      if (!start) start = 0;
      if (!end && end !== 0) end = this.length;
      if (targetStart >= target.length) targetStart = target.length;
      if (!targetStart) targetStart = 0;
      if (end > 0 && end < start) end = start;
      if (end === start) return 0;
      if (target.length === 0 || this.length === 0) return 0;
      if (targetStart < 0) {
        throw new RangeError("targetStart out of bounds");
      }
      if (start < 0 || start >= this.length) throw new RangeError("Index out of range");
      if (end < 0) throw new RangeError("sourceEnd out of bounds");
      if (end > this.length) end = this.length;
      if (target.length - targetStart < end - start) {
        end = target.length - targetStart + start;
      }
      const len = end - start;
      if (this === target && typeof Uint8Array.prototype.copyWithin === "function") {
        this.copyWithin(targetStart, start, end);
      } else {
        Uint8Array.prototype.set.call(
          target,
          this.subarray(start, end),
          targetStart
        );
      }
      return len;
    };
    Buffer.prototype.fill = function fill(val, start, end, encoding) {
      if (typeof val === "string") {
        if (typeof start === "string") {
          encoding = start;
          start = 0;
          end = this.length;
        } else if (typeof end === "string") {
          encoding = end;
          end = this.length;
        }
        if (encoding !== void 0 && typeof encoding !== "string") {
          throw new TypeError("encoding must be a string");
        }
        if (typeof encoding === "string" && !Buffer.isEncoding(encoding)) {
          throw new TypeError("Unknown encoding: " + encoding);
        }
        if (val.length === 1) {
          const code = val.charCodeAt(0);
          if (encoding === "utf8" && code < 128 || encoding === "latin1") {
            val = code;
          }
        }
      } else if (typeof val === "number") {
        val = val & 255;
      } else if (typeof val === "boolean") {
        val = Number(val);
      }
      if (start < 0 || this.length < start || this.length < end) {
        throw new RangeError("Out of range index");
      }
      if (end <= start) {
        return this;
      }
      start = start >>> 0;
      end = end === void 0 ? this.length : end >>> 0;
      if (!val) val = 0;
      let i;
      if (typeof val === "number") {
        for (i = start; i < end; ++i) {
          this[i] = val;
        }
      } else {
        const bytes = Buffer.isBuffer(val) ? val : Buffer.from(val, encoding);
        const len = bytes.length;
        if (len === 0) {
          throw new TypeError('The value "' + val + '" is invalid for argument "value"');
        }
        for (i = 0; i < end - start; ++i) {
          this[i + start] = bytes[i % len];
        }
      }
      return this;
    };
    var errors = {};
    function E(sym, getMessage, Base) {
      errors[sym] = class NodeError extends Base {
        constructor() {
          super();
          Object.defineProperty(this, "message", {
            value: getMessage.apply(this, arguments),
            writable: true,
            configurable: true
          });
          this.name = `${this.name} [${sym}]`;
          this.stack;
          delete this.name;
        }
        get code() {
          return sym;
        }
        set code(value) {
          Object.defineProperty(this, "code", {
            configurable: true,
            enumerable: true,
            value,
            writable: true
          });
        }
        toString() {
          return `${this.name} [${sym}]: ${this.message}`;
        }
      };
    }
    E(
      "ERR_BUFFER_OUT_OF_BOUNDS",
      function(name) {
        if (name) {
          return `${name} is outside of buffer bounds`;
        }
        return "Attempt to access memory outside buffer bounds";
      },
      RangeError
    );
    E(
      "ERR_INVALID_ARG_TYPE",
      function(name, actual) {
        return `The "${name}" argument must be of type number. Received type ${typeof actual}`;
      },
      TypeError
    );
    E(
      "ERR_OUT_OF_RANGE",
      function(str, range, input) {
        let msg = `The value of "${str}" is out of range.`;
        let received = input;
        if (Number.isInteger(input) && Math.abs(input) > 2 ** 32) {
          received = addNumericalSeparator(String(input));
        } else if (typeof input === "bigint") {
          received = String(input);
          if (input > BigInt(2) ** BigInt(32) || input < -(BigInt(2) ** BigInt(32))) {
            received = addNumericalSeparator(received);
          }
          received += "n";
        }
        msg += ` It must be ${range}. Received ${received}`;
        return msg;
      },
      RangeError
    );
    function addNumericalSeparator(val) {
      let res = "";
      let i = val.length;
      const start = val[0] === "-" ? 1 : 0;
      for (; i >= start + 4; i -= 3) {
        res = `_${val.slice(i - 3, i)}${res}`;
      }
      return `${val.slice(0, i)}${res}`;
    }
    function checkBounds(buf, offset, byteLength2) {
      validateNumber(offset, "offset");
      if (buf[offset] === void 0 || buf[offset + byteLength2] === void 0) {
        boundsError(offset, buf.length - (byteLength2 + 1));
      }
    }
    function checkIntBI(value, min, max, buf, offset, byteLength2) {
      if (value > max || value < min) {
        const n = typeof min === "bigint" ? "n" : "";
        let range;
        if (byteLength2 > 3) {
          if (min === 0 || min === BigInt(0)) {
            range = `>= 0${n} and < 2${n} ** ${(byteLength2 + 1) * 8}${n}`;
          } else {
            range = `>= -(2${n} ** ${(byteLength2 + 1) * 8 - 1}${n}) and < 2 ** ${(byteLength2 + 1) * 8 - 1}${n}`;
          }
        } else {
          range = `>= ${min}${n} and <= ${max}${n}`;
        }
        throw new errors.ERR_OUT_OF_RANGE("value", range, value);
      }
      checkBounds(buf, offset, byteLength2);
    }
    function validateNumber(value, name) {
      if (typeof value !== "number") {
        throw new errors.ERR_INVALID_ARG_TYPE(name, "number", value);
      }
    }
    function boundsError(value, length, type) {
      if (Math.floor(value) !== value) {
        validateNumber(value, type);
        throw new errors.ERR_OUT_OF_RANGE(type || "offset", "an integer", value);
      }
      if (length < 0) {
        throw new errors.ERR_BUFFER_OUT_OF_BOUNDS();
      }
      throw new errors.ERR_OUT_OF_RANGE(
        type || "offset",
        `>= ${type ? 1 : 0} and <= ${length}`,
        value
      );
    }
    var INVALID_BASE64_RE = /[^+/0-9A-Za-z-_]/g;
    function base64clean(str) {
      str = str.split("=")[0];
      str = str.trim().replace(INVALID_BASE64_RE, "");
      if (str.length < 2) return "";
      while (str.length % 4 !== 0) {
        str = str + "=";
      }
      return str;
    }
    function utf8ToBytes(string, units) {
      units = units || Infinity;
      let codePoint;
      const length = string.length;
      let leadSurrogate = null;
      const bytes = [];
      for (let i = 0; i < length; ++i) {
        codePoint = string.charCodeAt(i);
        if (codePoint > 55295 && codePoint < 57344) {
          if (!leadSurrogate) {
            if (codePoint > 56319) {
              if ((units -= 3) > -1) bytes.push(239, 191, 189);
              continue;
            } else if (i + 1 === length) {
              if ((units -= 3) > -1) bytes.push(239, 191, 189);
              continue;
            }
            leadSurrogate = codePoint;
            continue;
          }
          if (codePoint < 56320) {
            if ((units -= 3) > -1) bytes.push(239, 191, 189);
            leadSurrogate = codePoint;
            continue;
          }
          codePoint = (leadSurrogate - 55296 << 10 | codePoint - 56320) + 65536;
        } else if (leadSurrogate) {
          if ((units -= 3) > -1) bytes.push(239, 191, 189);
        }
        leadSurrogate = null;
        if (codePoint < 128) {
          if ((units -= 1) < 0) break;
          bytes.push(codePoint);
        } else if (codePoint < 2048) {
          if ((units -= 2) < 0) break;
          bytes.push(
            codePoint >> 6 | 192,
            codePoint & 63 | 128
          );
        } else if (codePoint < 65536) {
          if ((units -= 3) < 0) break;
          bytes.push(
            codePoint >> 12 | 224,
            codePoint >> 6 & 63 | 128,
            codePoint & 63 | 128
          );
        } else if (codePoint < 1114112) {
          if ((units -= 4) < 0) break;
          bytes.push(
            codePoint >> 18 | 240,
            codePoint >> 12 & 63 | 128,
            codePoint >> 6 & 63 | 128,
            codePoint & 63 | 128
          );
        } else {
          throw new Error("Invalid code point");
        }
      }
      return bytes;
    }
    function asciiToBytes(str) {
      const byteArray = [];
      for (let i = 0; i < str.length; ++i) {
        byteArray.push(str.charCodeAt(i) & 255);
      }
      return byteArray;
    }
    function utf16leToBytes(str, units) {
      let c, hi, lo;
      const byteArray = [];
      for (let i = 0; i < str.length; ++i) {
        if ((units -= 2) < 0) break;
        c = str.charCodeAt(i);
        hi = c >> 8;
        lo = c % 256;
        byteArray.push(lo);
        byteArray.push(hi);
      }
      return byteArray;
    }
    function base64ToBytes(str) {
      return base64.toByteArray(base64clean(str));
    }
    function blitBuffer(src, dst, offset, length) {
      let i;
      for (i = 0; i < length; ++i) {
        if (i + offset >= dst.length || i >= src.length) break;
        dst[i + offset] = src[i];
      }
      return i;
    }
    function isInstance(obj, type) {
      return obj instanceof type || obj != null && obj.constructor != null && obj.constructor.name != null && obj.constructor.name === type.name;
    }
    function numberIsNaN(obj) {
      return obj !== obj;
    }
    var hexSliceLookupTable = (function() {
      const alphabet = "0123456789abcdef";
      const table = new Array(256);
      for (let i = 0; i < 16; ++i) {
        const i16 = i * 16;
        for (let j = 0; j < 16; ++j) {
          table[i16 + j] = alphabet[i] + alphabet[j];
        }
      }
      return table;
    })();
    function defineBigIntMethod(fn) {
      return typeof BigInt === "undefined" ? BufferBigIntNotDefined : fn;
    }
    function BufferBigIntNotDefined() {
      throw new Error("BigInt not supported");
    }
  }
});

// node_modules/ts-khqr/lib/utils/crc16.js
var require_crc16 = __commonJS({
  "node_modules/ts-khqr/lib/utils/crc16.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.crc16 = void 0;
    var buffer_1 = require_buffer();
    var crcTable = [
      0,
      4129,
      8258,
      12387,
      16516,
      20645,
      24774,
      28903,
      33032,
      37161,
      41290,
      45419,
      49548,
      53677,
      57806,
      61935,
      4657,
      528,
      12915,
      8786,
      21173,
      17044,
      29431,
      25302,
      37689,
      33560,
      45947,
      41818,
      54205,
      50076,
      62463,
      58334,
      9314,
      13379,
      1056,
      5121,
      25830,
      29895,
      17572,
      21637,
      42346,
      46411,
      34088,
      38153,
      58862,
      62927,
      50604,
      54669,
      13907,
      9842,
      5649,
      1584,
      30423,
      26358,
      22165,
      18100,
      46939,
      42874,
      38681,
      34616,
      63455,
      59390,
      55197,
      51132,
      18628,
      22757,
      26758,
      30887,
      2112,
      6241,
      10242,
      14371,
      51660,
      55789,
      59790,
      63919,
      35144,
      39273,
      43274,
      47403,
      23285,
      19156,
      31415,
      27286,
      6769,
      2640,
      14899,
      10770,
      56317,
      52188,
      64447,
      60318,
      39801,
      35672,
      47931,
      43802,
      27814,
      31879,
      19684,
      23749,
      11298,
      15363,
      3168,
      7233,
      60846,
      64911,
      52716,
      56781,
      44330,
      48395,
      36200,
      40265,
      32407,
      28342,
      24277,
      20212,
      15891,
      11826,
      7761,
      3696,
      65439,
      61374,
      57309,
      53244,
      48923,
      44858,
      40793,
      36728,
      37256,
      33193,
      45514,
      41451,
      53516,
      49453,
      61774,
      57711,
      4224,
      161,
      12482,
      8419,
      20484,
      16421,
      28742,
      24679,
      33721,
      37784,
      41979,
      46042,
      49981,
      54044,
      58239,
      62302,
      689,
      4752,
      8947,
      13010,
      16949,
      21012,
      25207,
      29270,
      46570,
      42443,
      38312,
      34185,
      62830,
      58703,
      54572,
      50445,
      13538,
      9411,
      5280,
      1153,
      29798,
      25671,
      21540,
      17413,
      42971,
      47098,
      34713,
      38840,
      59231,
      63358,
      50973,
      55100,
      9939,
      14066,
      1681,
      5808,
      26199,
      30326,
      17941,
      22068,
      55628,
      51565,
      63758,
      59695,
      39368,
      35305,
      47498,
      43435,
      22596,
      18533,
      30726,
      26663,
      6336,
      2273,
      14466,
      10403,
      52093,
      56156,
      60223,
      64286,
      35833,
      39896,
      43963,
      48026,
      19061,
      23124,
      27191,
      31254,
      2801,
      6864,
      10931,
      14994,
      64814,
      60687,
      56684,
      52557,
      48554,
      44427,
      40424,
      36297,
      31782,
      27655,
      23652,
      19525,
      15522,
      11395,
      7392,
      3265,
      61215,
      65342,
      53085,
      57212,
      44955,
      49082,
      36825,
      40952,
      28183,
      32310,
      20053,
      24180,
      11923,
      16050,
      3793,
      7920
    ];
    function crc16(s) {
      let crc = 65535;
      let i;
      let j;
      const b = buffer_1.Buffer.from(s);
      for (i = 0; i < b.length; i++) {
        const c = b[i];
        j = (c ^ crc >> 8) & 255;
        crc = crcTable[j] ^ crc << 8;
      }
      const finalCheckSum = (crc ^ 0) & 65535;
      return getHexString(finalCheckSum);
    }
    exports.crc16 = crc16;
    function getHexString(crc) {
      let result = crc.toString(16).toUpperCase();
      if (result.length < 4) {
        result = 0 * (4 - result.length) + result;
      }
      const resultLength = 4 - result.length;
      if (result.length < 4)
        result = resultLength * 0 + result;
      return result;
    }
  }
});

// node_modules/ts-khqr/lib/utils/strings.js
var require_strings = __commonJS({
  "node_modules/ts-khqr/lib/utils/strings.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StringUtils = void 0;
    var StringUtils = class {
      static isEmpty(value) {
        if (value === void 0 || value === null) {
          return true;
        }
        if (typeof value === "string" && value === "") {
          return true;
        }
        return false;
      }
      static cutString(str) {
        const sliceIndex = 2;
        const tag = str.slice(0, sliceIndex);
        const length = Number(str.slice(sliceIndex, sliceIndex * 2));
        const value = str.slice(sliceIndex * 2, sliceIndex * 2 + length);
        const slicedString = str.slice(sliceIndex * 2 + length);
        return {
          tag,
          value,
          slicedString
        };
      }
    };
    exports.StringUtils = StringUtils;
  }
});

// node_modules/ts-khqr/lib/utils/response.js
var require_response = __commonJS({
  "node_modules/ts-khqr/lib/utils/response.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.response = void 0;
    var response = (data, error) => ({
      status: {
        code: error ? 1 : 0,
        errorCode: error ? error.code : null,
        message: error ? error.message : null
      },
      data
    });
    exports.response = response;
  }
});

// node_modules/ts-khqr/lib/utils/index.js
var require_utils = __commonJS({
  "node_modules/ts-khqr/lib/utils/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.response = exports.StringUtils = exports.crc16 = void 0;
    var crc16_1 = require_crc16();
    Object.defineProperty(exports, "crc16", { enumerable: true, get: function() {
      return crc16_1.crc16;
    } });
    var strings_1 = require_strings();
    Object.defineProperty(exports, "StringUtils", { enumerable: true, get: function() {
      return strings_1.StringUtils;
    } });
    var response_1 = require_response();
    Object.defineProperty(exports, "response", { enumerable: true, get: function() {
      return response_1.response;
    } });
  }
});

// node_modules/ts-khqr/lib/models/tags/base/tag-length-string.js
var require_tag_length_string = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/base/tag-length-string.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TagLengthString = void 0;
    var TagLengthString = class {
      constructor(tag, value) {
        this.tag = tag;
        this.value = value;
        const length = String(value).length;
        this.length = length < 10 ? `0${length}` : String(length);
      }
      toString() {
        return `${this.tag}${this.length}${this.value}`;
      }
    };
    exports.TagLengthString = TagLengthString;
  }
});

// node_modules/ts-khqr/lib/models/tags/point-of-initiation-method.js
var require_point_of_initiation_method = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/point-of-initiation-method.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PointOfInitiationMethod = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var PointOfInitiationMethod = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > 2) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.POINT_INITIATION_LENGTH_INVALID);
        }
        if (value !== constants_1.EMV.STATIC_QR && value !== constants_1.EMV.DYNAMIC_QR) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.POINT_OF_INITIATION_METHOD_INVALID);
        }
        super(tag, String(value));
      }
    };
    exports.PointOfInitiationMethod = PointOfInitiationMethod;
  }
});

// node_modules/ts-khqr/lib/models/tags/payload-format-indicator.js
var require_payload_format_indicator = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/payload-format-indicator.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PayloadFormatIndicator = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var PayloadFormatIndicator = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.PAYLOAD_FORMAT_INDICATOR_TAG_REQUIRED);
        } else if (String(value).length > 2) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.PAYLOAD_FORMAT_INDICATOR_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    exports.PayloadFormatIndicator = PayloadFormatIndicator;
  }
});

// node_modules/ts-khqr/lib/models/tags/global-unique-identifier.js
var require_global_unique_identifier = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/global-unique-identifier.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.GlobalUniqueIdentifier = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var GlobalUniqueIdentifier = class extends tag_length_string_1.TagLengthString {
      constructor(tag, obj) {
        if (!obj || typeof obj !== "object") {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_TYPE_REQUIRED);
        }
        const bakongAccountID = new BakongAccountID(constants_1.EMV.BAKONG_ACCOUNT_IDENTIFIER, obj.bakongAccountID);
        let globalUniqueIdentifier = bakongAccountID.toString();
        if (obj.isMerchant) {
          const merchantID = new MerchantID(constants_1.EMV.MERCHANT_ACCOUNT_INFORMATION_MERCHANT_ID, obj.merchantID);
          const acquiringBank = new AcquiringBank(constants_1.EMV.MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK, obj.acquiringBank);
          if (obj.merchantID !== void 0 && obj.merchantID !== null) {
            globalUniqueIdentifier += merchantID.toString();
          }
          if (obj.acquiringBank !== void 0 && obj.acquiringBank !== null) {
            globalUniqueIdentifier += acquiringBank.toString();
          }
        } else {
          if (!utils_1.StringUtils.isEmpty(obj.accountInformation)) {
            const accInformation = new AccountInformation(constants_1.EMV.INDIVIDUAL_ACCOUNT_INFORMATION, String(obj.accountInformation));
            globalUniqueIdentifier += accInformation.toString();
          }
          if (!utils_1.StringUtils.isEmpty(obj.acquiringBank)) {
            const acquiringBank = new AcquiringBank(constants_1.EMV.MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK, obj.acquiringBank);
            globalUniqueIdentifier += acquiringBank.toString();
          }
        }
        super(tag, globalUniqueIdentifier);
      }
    };
    exports.GlobalUniqueIdentifier = GlobalUniqueIdentifier;
    var BakongAccountID = class extends tag_length_string_1.TagLengthString {
      constructor(tag, bakongAccountID) {
        if (utils_1.StringUtils.isEmpty(bakongAccountID)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.BAKONG_ACCOUNT_ID_REQUIRED);
        }
        const bakongAccountDivide = String(bakongAccountID).split("@");
        if (String(bakongAccountID).length > constants_1.EMV.INVALID_LENGTH.BAKONG_ACCOUNT) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.BAKONG_ACCOUNT_ID_LENGTH_INVALID);
        } else if (bakongAccountDivide.length < 2) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.BAKONG_ACCOUNT_ID_INVALID);
        }
        super(tag, String(bakongAccountID));
      }
    };
    var MerchantID = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_ID_REQUIRED);
        } else if (String(value).length > constants_1.EMV.INVALID_LENGTH.MERCHANT_ID) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_ID_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    var AcquiringBank = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.ACQUIRING_BANK_REQUIRED);
        } else if (String(value).length > constants_1.EMV.INVALID_LENGTH.ACQUIRING_BANK) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.ACQUIRING_BANK_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    var AccountInformation = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (value.length > constants_1.EMV.INVALID_LENGTH.ACCOUNT_INFORMATION) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.ACCOUNT_INFORMATION_LENGTH_INVALID);
        }
        super(tag, value);
      }
    };
  }
});

// node_modules/ts-khqr/lib/models/tags/transaction-currency.js
var require_transaction_currency = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/transaction-currency.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TransactionCurrency = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var TransactionCurrency = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (value === void 0 || value === null) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.CURRENCY_TYPE_REQUIRED);
        } else if (String(value).length > 3) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.TRANSACTION_CURRENCY_LENGTH_INVALID);
        } else if (!Object.values(constants_1.CURRENCY).includes(value)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.UNSUPPORTED_CURRENCY);
        }
        super(tag, value);
      }
    };
    exports.TransactionCurrency = TransactionCurrency;
  }
});

// node_modules/ts-khqr/lib/models/tags/transaction-amount.js
var require_transaction_amount = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/transaction-amount.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TransactionAmount = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var TransactionAmount = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > constants_1.EMV.INVALID_LENGTH.AMOUNT || String(value).includes("-")) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.TRANSACTION_AMOUNT_INVALID);
        }
        super(tag, String(value));
      }
    };
    exports.TransactionAmount = TransactionAmount;
  }
});

// node_modules/ts-khqr/lib/models/tags/merchant-category-code.js
var require_merchant_category_code = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/merchant-category-code.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MerchantCategoryCode = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var MerchantCategoryCode = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_CATEGORY_TAG_REQUIRED);
        } else if (String(value).length > constants_1.EMV.INVALID_LENGTH.MERCHANT_CATEGORY_CODE) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_CODE_LENGTH_INVALID);
        } else if (!/^\d+$/.test(String(value))) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.INVALID_MERCHANT_CATEGORY_CODE);
        } else {
          const mcc = parseInt(String(value), 10);
          if (mcc < 0 || mcc > 9999) {
            throw (0, utils_1.response)(null, constants_1.ERROR_CODE.INVALID_MERCHANT_CATEGORY_CODE);
          }
        }
        super(tag, String(value));
      }
    };
    exports.MerchantCategoryCode = MerchantCategoryCode;
  }
});

// node_modules/ts-khqr/lib/models/tags/country-code.js
var require_country_code = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/country-code.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CountryCode = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var CountryCode = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.COUNTRY_CODE_TAG_REQUIRED);
        } else if (String(value).length > constants_1.EMV.INVALID_LENGTH.COUNTRY_CODE) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.COUNTRY_CODE_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    exports.CountryCode = CountryCode;
  }
});

// node_modules/ts-khqr/lib/models/tags/merchant-name.js
var require_merchant_name = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/merchant-name.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MerchantName = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var MerchantName = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_NAME_REQUIRED);
        } else if (String(value).length > constants_1.EMV.INVALID_LENGTH.MERCHANT_NAME) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_NAME_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    exports.MerchantName = MerchantName;
  }
});

// node_modules/ts-khqr/lib/models/tags/merchant-city.js
var require_merchant_city = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/merchant-city.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MerchantCity = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var MerchantCity = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_CITY_TAG_REQUIRED);
        } else if (String(value).length > constants_1.EMV.INVALID_LENGTH.MERCHANT_CITY) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_CITY_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    exports.MerchantCity = MerchantCity;
  }
});

// node_modules/ts-khqr/lib/models/tags/time-stamp.js
var require_time_stamp = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/time-stamp.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TimeStamp = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var TimeStamp = class extends tag_length_string_1.TagLengthString {
      constructor(tag, timestamp, poi) {
        const creationTimestamp = Number(timestamp === null || timestamp === void 0 ? void 0 : timestamp.creationTimestamp);
        const expirationTimestamp = Number(timestamp === null || timestamp === void 0 ? void 0 : timestamp.expirationTimestamp);
        if (poi === constants_1.EMV.DYNAMIC_QR) {
          if (!timestamp || !expirationTimestamp) {
            throw (0, utils_1.response)(null, constants_1.ERROR_CODE.EXPIRATION_TIMESTAMP_REQUIRED);
          }
          if (String(expirationTimestamp).length !== constants_1.EMV.INVALID_LENGTH.TIMESTAMP) {
            throw (0, utils_1.response)(null, constants_1.ERROR_CODE.EXPIRATION_TIMESTAMP_LENGTH_INVALID);
          }
          if (isNaN(new Date(expirationTimestamp).getTime())) {
            throw (0, utils_1.response)(null, constants_1.ERROR_CODE.INVALID_DYNAMIC_KHQR);
          }
          if (expirationTimestamp < creationTimestamp) {
            throw (0, utils_1.response)(null, constants_1.ERROR_CODE.EXPIRATION_TIMESTAMP_IN_THE_PAST);
          }
          if (expirationTimestamp < Date.now()) {
            throw (0, utils_1.response)(null, constants_1.ERROR_CODE.KHQR_EXPIRED);
          }
        }
        let timestampString = "";
        const createdTimestamp = new TimeStampMiliSecond(constants_1.EMV.CREATION_TIMESTAMP, creationTimestamp);
        timestampString += createdTimestamp.toString();
        const expiredTimestamp = new TimeStampMiliSecond(constants_1.EMV.EXPIRATION_TIMESTAMP, expirationTimestamp);
        timestampString += expiredTimestamp.toString();
        super(tag, timestampString);
      }
    };
    exports.TimeStamp = TimeStamp;
    var TimeStampMiliSecond = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        super(tag, value);
      }
    };
  }
});

// node_modules/ts-khqr/lib/models/tags/additional-data.js
var require_additional_data = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/additional-data.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AdditionalData = void 0;
    var constants_1 = require_constants();
    var tag_length_string_1 = require_tag_length_string();
    var utils_1 = require_utils();
    var AdditionalData = class extends tag_length_string_1.TagLengthString {
      constructor(tag, additionalData = {}) {
        let additionalDataString = "";
        if (additionalData.billNumber !== void 0 && additionalData.billNumber !== null) {
          const billNumber = new BillNumber(constants_1.EMV.BILLNUMBER_TAG, additionalData.billNumber);
          additionalDataString += billNumber.toString();
        }
        if (additionalData.mobileNumber !== void 0 && additionalData.mobileNumber !== null) {
          const mobileNumber = new MobileNumber(constants_1.EMV.ADDITIONAL_DATA_FIELD_MOBILE_NUMBER, additionalData.mobileNumber);
          additionalDataString += mobileNumber.toString();
        }
        if (additionalData.storeLabel !== void 0 && additionalData.storeLabel !== null) {
          const storeLabel = new StoreLabel(constants_1.EMV.STORELABEL_TAG, additionalData.storeLabel);
          additionalDataString += storeLabel.toString();
        }
        if (additionalData.terminalLabel !== void 0 && additionalData.terminalLabel !== null) {
          const terminalLabel = new TerminalLabel(constants_1.EMV.TERMINAL_TAG, additionalData.terminalLabel);
          additionalDataString += terminalLabel.toString();
        }
        if (additionalData.purposeOfTransaction !== void 0 && additionalData.purposeOfTransaction !== null) {
          const purposeOfTransaction = new PurposeOfTransaction(constants_1.EMV.PURPOSE_OF_TRANSACTION, additionalData.purposeOfTransaction);
          additionalDataString += purposeOfTransaction.toString();
        }
        super(tag, additionalDataString);
      }
    };
    exports.AdditionalData = AdditionalData;
    var BillNumber = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > constants_1.EMV.INVALID_LENGTH.BILL_NUMBER) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.BILL_NUMBER_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    var StoreLabel = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > constants_1.EMV.INVALID_LENGTH.STORE_LABEL) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.STORE_LABEL_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    var TerminalLabel = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > constants_1.EMV.INVALID_LENGTH.TERMINAL_LABEL) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.TERMINAL_LABEL_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    var MobileNumber = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > constants_1.EMV.INVALID_LENGTH.MOBILE_NUMBER) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MOBILE_NUMBER_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    var PurposeOfTransaction = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > constants_1.EMV.INVALID_LENGTH.PURPOSE_OF_TRANSACTION)
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.PURPOSE_OF_TRANSACTION_LENGTH_INVALID);
        super(tag, String(value));
      }
    };
  }
});

// node_modules/ts-khqr/lib/models/tags/crc.js
var require_crc = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/crc.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CRC = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var CRC = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value)) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.CRC_TAG_REQUIRED);
        } else if (String(value).length > 4) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.CRC_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    exports.CRC = CRC;
  }
});

// node_modules/ts-khqr/lib/models/tags/unionpay-merchant-account.js
var require_unionpay_merchant_account = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/unionpay-merchant-account.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.UnionpayMerchantAccount = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var UnionpayMerchantAccount = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (String(value).length > constants_1.EMV.INVALID_LENGTH.UPI_MERCHANT) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.UPI_ACCOUNT_INFORMATION_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    exports.UnionpayMerchantAccount = UnionpayMerchantAccount;
  }
});

// node_modules/ts-khqr/lib/models/tags/merchant-information-language-template.js
var require_merchant_information_language_template = __commonJS({
  "node_modules/ts-khqr/lib/models/tags/merchant-information-language-template.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MerchantInformationLanguageTemplate = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var tag_length_string_1 = require_tag_length_string();
    var MerchantInformationLanguageTemplate = class extends tag_length_string_1.TagLengthString {
      constructor(tag, obj = {}) {
        if (obj.languagePreference && !obj.merchantNameAlternateLanguage) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_NAME_ALTERNATE_LANGUAGE_REQUIRED);
        }
        let merchantInformationLanguageTemplateString = "";
        const languagePreference = new LanguagePreference(constants_1.EMV.LANGUAGE_PREFERENCE, obj.languagePreference);
        merchantInformationLanguageTemplateString += languagePreference.toString();
        if (obj.merchantNameAlternateLanguage !== void 0 && obj.merchantNameAlternateLanguage !== null) {
          const merchantNameAlternateLanguage = new MerchantNameAlternateLanguage(constants_1.EMV.MERCHANT_NAME_ALTERNATE_LANGUAGE, obj.merchantNameAlternateLanguage);
          merchantInformationLanguageTemplateString += merchantNameAlternateLanguage.toString();
        }
        if (obj.merchantCityAlternateLanguage !== void 0 && obj.merchantCityAlternateLanguage !== null) {
          const merchantCityAlternateLanguage = new MerchantCityAlternateLanguage(constants_1.EMV.MERCHANT_CITY_ALTERNATE_LANGUAGE, obj.merchantCityAlternateLanguage);
          merchantInformationLanguageTemplateString += merchantCityAlternateLanguage.toString();
        }
        super(tag, merchantInformationLanguageTemplateString);
      }
    };
    exports.MerchantInformationLanguageTemplate = MerchantInformationLanguageTemplate;
    var LanguagePreference = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > constants_1.EMV.INVALID_LENGTH.LANGUAGE_PREFERENCE) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.LANGUAGE_PREFERENCE_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    var MerchantNameAlternateLanguage = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > constants_1.EMV.INVALID_LENGTH.MERCHANT_NAME_ALTERNATE_LANGUAGE) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_NAME_ALTERNATE_LANGUAGE_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
    var MerchantCityAlternateLanguage = class extends tag_length_string_1.TagLengthString {
      constructor(tag, value) {
        if (utils_1.StringUtils.isEmpty(value) || String(value).length > constants_1.EMV.INVALID_LENGTH.MERCHANT_CITY_ALTERNATE_LANGUAGE) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.MERCHANT_CITY_ALTERNATE_LANGUAGE_LENGTH_INVALID);
        }
        super(tag, String(value));
      }
    };
  }
});

// node_modules/ts-khqr/lib/models/index.js
var require_models = __commonJS({
  "node_modules/ts-khqr/lib/models/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MerchantInformationLanguageTemplate = exports.UnionpayMerchantAccount = exports.CRC = exports.AdditionalData = exports.TimeStamp = exports.MerchantCity = exports.MerchantName = exports.CountryCode = exports.MerchantCategoryCode = exports.TransactionAmount = exports.TransactionCurrency = exports.GlobalUniqueIdentifier = exports.PayloadFormatIndicator = exports.PointOfInitiationMethod = void 0;
    var point_of_initiation_method_1 = require_point_of_initiation_method();
    Object.defineProperty(exports, "PointOfInitiationMethod", { enumerable: true, get: function() {
      return point_of_initiation_method_1.PointOfInitiationMethod;
    } });
    var payload_format_indicator_1 = require_payload_format_indicator();
    Object.defineProperty(exports, "PayloadFormatIndicator", { enumerable: true, get: function() {
      return payload_format_indicator_1.PayloadFormatIndicator;
    } });
    var global_unique_identifier_1 = require_global_unique_identifier();
    Object.defineProperty(exports, "GlobalUniqueIdentifier", { enumerable: true, get: function() {
      return global_unique_identifier_1.GlobalUniqueIdentifier;
    } });
    var transaction_currency_1 = require_transaction_currency();
    Object.defineProperty(exports, "TransactionCurrency", { enumerable: true, get: function() {
      return transaction_currency_1.TransactionCurrency;
    } });
    var transaction_amount_1 = require_transaction_amount();
    Object.defineProperty(exports, "TransactionAmount", { enumerable: true, get: function() {
      return transaction_amount_1.TransactionAmount;
    } });
    var merchant_category_code_1 = require_merchant_category_code();
    Object.defineProperty(exports, "MerchantCategoryCode", { enumerable: true, get: function() {
      return merchant_category_code_1.MerchantCategoryCode;
    } });
    var country_code_1 = require_country_code();
    Object.defineProperty(exports, "CountryCode", { enumerable: true, get: function() {
      return country_code_1.CountryCode;
    } });
    var merchant_name_1 = require_merchant_name();
    Object.defineProperty(exports, "MerchantName", { enumerable: true, get: function() {
      return merchant_name_1.MerchantName;
    } });
    var merchant_city_1 = require_merchant_city();
    Object.defineProperty(exports, "MerchantCity", { enumerable: true, get: function() {
      return merchant_city_1.MerchantCity;
    } });
    var time_stamp_1 = require_time_stamp();
    Object.defineProperty(exports, "TimeStamp", { enumerable: true, get: function() {
      return time_stamp_1.TimeStamp;
    } });
    var additional_data_1 = require_additional_data();
    Object.defineProperty(exports, "AdditionalData", { enumerable: true, get: function() {
      return additional_data_1.AdditionalData;
    } });
    var crc_1 = require_crc();
    Object.defineProperty(exports, "CRC", { enumerable: true, get: function() {
      return crc_1.CRC;
    } });
    var unionpay_merchant_account_1 = require_unionpay_merchant_account();
    Object.defineProperty(exports, "UnionpayMerchantAccount", { enumerable: true, get: function() {
      return unionpay_merchant_account_1.UnionpayMerchantAccount;
    } });
    var merchant_information_language_template_1 = require_merchant_information_language_template();
    Object.defineProperty(exports, "MerchantInformationLanguageTemplate", { enumerable: true, get: function() {
      return merchant_information_language_template_1.MerchantInformationLanguageTemplate;
    } });
  }
});

// node_modules/ts-khqr/lib/constants/index.js
var require_constants = __commonJS({
  "node_modules/ts-khqr/lib/constants/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TAG = exports.COUNTRY = exports.CURRENCY = exports.KHQR_SUBTAG = exports.KHQR_TAG = exports.ERROR_CODE = exports.EMV = void 0;
    var models_1 = require_models();
    exports.EMV = Object.freeze({
      PAYLOAD_FORMAT_INDICATOR: "00",
      POINT_OF_INITIATION_METHOD: "01",
      STATIC_QR: "11",
      DYNAMIC_QR: "12",
      MERCHANT_ACCOUNT_INFORMATION_INDIVIDUAL: "29",
      MERCHANT_ACCOUNT_INFORMATION_MERCHANT: "30",
      BAKONG_ACCOUNT_IDENTIFIER: "00",
      MERCHANT_ACCOUNT_INFORMATION_MERCHANT_ID: "01",
      INDIVIDUAL_ACCOUNT_INFORMATION: "01",
      MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK: "02",
      MERCHANT_CATEGORY_CODE: "52",
      TRANSACTION_CURRENCY: "53",
      TRANSACTION_AMOUNT: "54",
      COUNTRY_CODE: "58",
      MERCHANT_NAME: "59",
      MERCHANT_CITY: "60",
      CRC: "63",
      CRC_LENGTH: "04",
      ADDITIONAL_DATA_TAG: "62",
      BILLNUMBER_TAG: "01",
      ADDITIONAL_DATA_FIELD_MOBILE_NUMBER: "02",
      STORELABEL_TAG: "03",
      TERMINAL_TAG: "07",
      PURPOSE_OF_TRANSACTION: "08",
      TIMESTAMP_TAG: "99",
      CREATION_TIMESTAMP: "00",
      EXPIRATION_TIMESTAMP: "01",
      MERCHANT_INFORMATION_LANGUAGE_TEMPLATE: "64",
      LANGUAGE_PREFERENCE: "00",
      MERCHANT_NAME_ALTERNATE_LANGUAGE: "01",
      MERCHANT_CITY_ALTERNATE_LANGUAGE: "02",
      UNIONPAY_MERCHANT_ACCOUNT: "15",
      // default value for KHQR
      DEFAULT_PAYLOAD_FORMAT_INDICATOR: "01",
      DEFAULT_MERCHANT_CATEGORY_CODE: "5999",
      DEFAULT_TRANSACTION_AMOUNT: "0",
      DEFAULT_COUNTRY_CODE: "KH",
      DEFAULT_MERCHANT_CITY: "Phnom Penh",
      INVALID_LENGTH: {
        KHQR: 12,
        MERCHANT_NAME: 25,
        BAKONG_ACCOUNT: 32,
        AMOUNT: 13,
        COUNTRY_CODE: 3,
        MERCHANT_CATEGORY_CODE: 4,
        MERCHANT_CITY: 15,
        TIMESTAMP: 13,
        TRANSACTION_AMOUNT: 14,
        TRANSACTION_CURRENCY: 3,
        BILL_NUMBER: 25,
        STORE_LABEL: 25,
        TERMINAL_LABEL: 25,
        PURPOSE_OF_TRANSACTION: 25,
        MERCHANT_ID: 32,
        ACQUIRING_BANK: 32,
        MOBILE_NUMBER: 25,
        ACCOUNT_INFORMATION: 32,
        MERCHANT_INFORMATION_LANGUAGE_TEMPLATE: 99,
        UPI_MERCHANT: 99,
        LANGUAGE_PREFERENCE: 2,
        MERCHANT_NAME_ALTERNATE_LANGUAGE: 25,
        MERCHANT_CITY_ALTERNATE_LANGUAGE: 15
      }
    });
    exports.ERROR_CODE = Object.freeze({
      BAKONG_ACCOUNT_ID_REQUIRED: {
        code: 1,
        message: "Bakong Account ID cannot be null or empty"
      },
      MERCHANT_NAME_REQUIRED: {
        code: 2,
        message: "Merchant name cannot be null or empty"
      },
      BAKONG_ACCOUNT_ID_INVALID: {
        code: 3,
        message: "Bakong Account ID is invalid"
      },
      TRANSACTION_AMOUNT_INVALID: {
        code: 4,
        message: "Amount is invalid"
      },
      MERCHANT_TYPE_REQUIRED: {
        code: 5,
        message: "Merchant type cannot be null or empty"
      },
      BAKONG_ACCOUNT_ID_LENGTH_INVALID: {
        code: 6,
        message: "Bakong Account ID Length is Invalid"
      },
      MERCHANT_NAME_LENGTH_INVALID: {
        code: 7,
        message: "Merchant Name Length is invalid"
      },
      KHQR_INVALID: {
        code: 8,
        message: "KHQR provided is invalid"
      },
      CURRENCY_TYPE_REQUIRED: {
        code: 9,
        message: "Currency type cannot be null or empty"
      },
      BILL_NUMBER_LENGTH_INVALID: {
        code: 10,
        message: "Bill Name Length is invalid"
      },
      STORE_LABEL_LENGTH_INVALID: {
        code: 11,
        message: "Store Label Length is invalid"
      },
      TERMINAL_LABEL_LENGTH_INVALID: {
        code: 12,
        message: "Terminal Label Length is invalid"
      },
      CONNECTION_TIMEOUT: {
        code: 13,
        message: "Cannot reach Bakong Open API service. Please check internet connection"
      },
      INVALID_DEEP_LINK_SOURCE_INFO: {
        code: 14,
        message: "Source Info for Deep Link is invalid"
      },
      INTERNAL_SREVER: {
        code: 15,
        message: "Internal server error"
      },
      PAYLOAD_FORMAT_INDICATOR_LENGTH_INVALID: {
        code: 16,
        message: "Payload Format indicator Length is invalid"
      },
      POINT_INITIATION_LENGTH_INVALID: {
        code: 17,
        message: "Point of initiation Length is invalid"
      },
      MERCHANT_CODE_LENGTH_INVALID: {
        code: 18,
        message: "Merchant code Length is invalid"
      },
      TRANSACTION_CURRENCY_LENGTH_INVALID: {
        code: 19,
        message: "Transaction currency Length is invalid"
      },
      COUNTRY_CODE_LENGTH_INVALID: {
        code: 20,
        message: "Country code Length is invalid"
      },
      MERCHANT_CITY_LENGTH_INVALID: {
        code: 21,
        message: "Merchant city Length is invalid"
      },
      CRC_LENGTH_INVALID: {
        code: 22,
        message: "CRC Length is invalid"
      },
      PAYLOAD_FORMAT_INDICATOR_TAG_REQUIRED: {
        code: 23,
        message: "Payload format indicator tag required"
      },
      CRC_TAG_REQUIRED: {
        code: 24,
        message: "CRC tag required"
      },
      MERCHANT_CATEGORY_TAG_REQUIRED: {
        code: 25,
        message: "Merchant category tag required"
      },
      COUNTRY_CODE_TAG_REQUIRED: {
        code: 26,
        message: "Country Code cannot be null or empty"
      },
      MERCHANT_CITY_TAG_REQUIRED: {
        code: 27,
        message: "Merchant City cannot be null or empty"
      },
      UNSUPPORTED_CURRENCY: {
        code: 28,
        message: "Unsupported currency"
      },
      INVALID_DEEP_LINK_URL: {
        code: 29,
        message: "Deep Link URL is not valid"
      },
      MERCHANT_ID_REQUIRED: {
        code: 30,
        message: "Merchant ID cannot be null or empty"
      },
      ACQUIRING_BANK_REQUIRED: {
        code: 31,
        message: "Acquiring Bank cannot be null or empty"
      },
      MERCHANT_ID_LENGTH_INVALID: {
        code: 32,
        message: "Merchant ID Length is invalid"
      },
      ACQUIRING_BANK_LENGTH_INVALID: {
        code: 33,
        message: "Acquiring Bank Length is invalid"
      },
      MOBILE_NUMBER_LENGTH_INVALID: {
        code: 34,
        message: "Mobile Number Length is invalid"
      },
      ACCOUNT_INFORMATION_LENGTH_INVALID: {
        code: 35,
        message: "Account Information Length is invalid"
      },
      TAG_NOT_IN_ORDER: {
        code: 36,
        message: "Tag is not in order"
      },
      LANGUAGE_PREFERENCE_REQUIRED: {
        code: 37,
        message: "Language Preference cannot be null or empty"
      },
      LANGUAGE_PREFERENCE_LENGTH_INVALID: {
        code: 38,
        message: "Language Preference Length is invalid"
      },
      MERCHANT_NAME_ALTERNATE_LANGUAGE_REQUIRED: {
        code: 39,
        message: "Merchant Name Alternate Language cannot be null or empty"
      },
      MERCHANT_NAME_ALTERNATE_LANGUAGE_LENGTH_INVALID: {
        code: 40,
        message: "Merchant Name Alternate Language Length is invalid"
      },
      MERCHANT_CITY_ALTERNATE_LANGUAGE_LENGTH_INVALID: {
        code: 41,
        message: "Merchant City Alternate Language Length is invalid"
      },
      PURPOSE_OF_TRANSACTION_LENGTH_INVALID: {
        code: 42,
        message: "Purpose of Transaction Length is invalid"
      },
      UPI_ACCOUNT_INFORMATION_LENGTH_INVALID: {
        code: 43,
        message: "Upi Account Information Length is invalid"
      },
      UPI_ACCOUNT_INFORMATION_INVALID_CURRENCY: {
        code: 44,
        message: "Upi Account Information Length does not accept USD"
      },
      EXPIRATION_TIMESTAMP_REQUIRED: {
        code: 45,
        message: "Expiration timestamp is required for dynamic KHQR"
      },
      KHQR_EXPIRED: {
        code: 46,
        message: "This dynamic KHQR has expired"
      },
      INVALID_DYNAMIC_KHQR: {
        code: 47,
        message: "This dynamic KHQR has invalid field transaction amount"
      },
      POINT_OF_INITIATION_METHOD_INVALID: {
        code: 48,
        message: "Point of Initiation Method is invalid"
      },
      EXPIRATION_TIMESTAMP_LENGTH_INVALID: {
        code: 49,
        message: "Expiration timestamp length is invalid"
      },
      EXPIRATION_TIMESTAMP_IN_THE_PAST: {
        code: 50,
        message: "Expiration timestamp is in the past"
      },
      INVALID_MERCHANT_CATEGORY_CODE: {
        code: 51,
        message: "Invalid merchant category code"
      }
    });
    exports.KHQR_TAG = [
      {
        tag: "00",
        type: "payloadFormatIndicator",
        required: true,
        instance: models_1.PayloadFormatIndicator
      },
      {
        tag: "01",
        type: "pointofInitiationMethod",
        required: false,
        instance: models_1.PointOfInitiationMethod
      },
      {
        tag: "15",
        type: "unionPayMerchant",
        required: false,
        instance: models_1.UnionpayMerchantAccount
      },
      {
        sub: true,
        tag: "29",
        type: "globalUnqiueIdentifier",
        required: true,
        instance: models_1.GlobalUniqueIdentifier
      },
      {
        tag: "52",
        type: "merchantCategoryCode",
        required: true,
        instance: models_1.MerchantCategoryCode
      },
      {
        tag: "53",
        type: "transactionCurrency",
        required: true,
        instance: models_1.TransactionCurrency
      },
      {
        tag: "54",
        type: "transactionAmount",
        required: false,
        instance: models_1.TransactionAmount
      },
      {
        tag: "58",
        type: "countryCode",
        required: true,
        instance: models_1.CountryCode
      },
      {
        tag: "59",
        type: "merchantName",
        required: true,
        instance: models_1.MerchantName
      },
      {
        tag: "60",
        type: "merchantCity",
        required: true,
        instance: models_1.MerchantCity
      },
      {
        tag: "62",
        sub: true,
        type: "additionalData",
        required: false,
        instance: models_1.AdditionalData
      },
      {
        tag: "64",
        sub: true,
        type: "merchantInformationLanguageTemplate",
        required: false,
        instance: models_1.MerchantInformationLanguageTemplate
      },
      {
        tag: "99",
        sub: true,
        type: "timestamp",
        required: false,
        instance: models_1.TimeStamp
      },
      {
        tag: "63",
        type: "crc",
        required: true,
        instance: models_1.CRC
      }
    ];
    exports.KHQR_SUBTAG = Object.freeze({
      input: [
        {
          tag: "29",
          data: {
            bakongAccountID: null,
            accountInformation: null
          }
        },
        {
          tag: "30",
          data: {
            bakongAccountID: null,
            merchantID: null,
            acquiringBank: null
          }
        },
        {
          tag: "62",
          data: {
            billNumber: null,
            mobileNumber: null,
            storeLabel: null,
            terminalLabel: null,
            purposeOfTransaction: null
          }
        },
        {
          tag: "64",
          data: {
            languagePreference: null,
            merchantNameAlternateLanguage: null,
            merchantCityAlternateLanguage: null
          }
        },
        {
          tag: "99",
          data: {
            creationTimestamp: null,
            expirationTimestamp: null
          }
        }
      ],
      compare: [
        {
          tag: "29",
          subTag: exports.EMV.BAKONG_ACCOUNT_IDENTIFIER,
          name: "bakongAccountID"
        },
        {
          tag: "29",
          subTag: exports.EMV.MERCHANT_ACCOUNT_INFORMATION_MERCHANT_ID,
          name: "accountInformation"
        },
        {
          tag: "29",
          subTag: exports.EMV.MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK,
          name: "acquiringBank"
        },
        {
          tag: "62",
          subTag: exports.EMV.BILLNUMBER_TAG,
          name: "billNumber"
        },
        {
          tag: "62",
          subTag: exports.EMV.ADDITIONAL_DATA_FIELD_MOBILE_NUMBER,
          name: "mobileNumber"
        },
        {
          tag: "62",
          subTag: exports.EMV.STORELABEL_TAG,
          name: "storeLabel"
        },
        {
          tag: "62",
          subTag: exports.EMV.PURPOSE_OF_TRANSACTION,
          name: "purposeOfTransaction"
        },
        {
          tag: "62",
          subTag: exports.EMV.TERMINAL_TAG,
          name: "terminalLabel"
        },
        {
          tag: "64",
          subTag: exports.EMV.LANGUAGE_PREFERENCE,
          name: "languagePreference"
        },
        {
          tag: "64",
          subTag: exports.EMV.MERCHANT_NAME_ALTERNATE_LANGUAGE,
          name: "merchantNameAlternateLanguage"
        },
        {
          tag: "64",
          subTag: exports.EMV.MERCHANT_CITY_ALTERNATE_LANGUAGE,
          name: "merchantCityAlternateLanguage"
        },
        {
          tag: "99",
          subTag: exports.EMV.CREATION_TIMESTAMP,
          name: "creationTimestamp"
        },
        {
          tag: "99",
          subTag: exports.EMV.EXPIRATION_TIMESTAMP,
          name: "expirationTimestamp"
        }
      ]
    });
    exports.CURRENCY = Object.freeze({
      USD: "840",
      KHR: "116"
      // THB: '764',
      // LAK: '418',
      // VND: '704',
      // MYR: '458',
      // MMK: '104',
      // BND: '096',
      // PHP: '608',
      // SGD: '702',
      // IDR: '360',
      // INR: '356',
      // CNY: '156',
      // AUD: '036',
      // CZK: '203',
      // EUR: '978',
    });
    exports.COUNTRY = Object.freeze({
      KH: "KH"
      // TH: 'TH',
      // LA: 'LA',
      // VN: 'VN',
      // MY: 'MY',
      // MM: 'MM',
      // BN: 'BN',
      // PH: 'PH',
      // SG: 'SG',
      // ID: 'ID',
      // IN: 'IN',
      // CN: 'CN',
      // AU: 'AU',
      // CZ: 'CZ',
      // FR: 'FR',
    });
    exports.TAG = Object.freeze({
      MERCHANT: "30",
      INDIVIDUAL: "29"
      // TAG_02: '02',
      // TAG_03: '03',
      // TAG_04: '04',
      // TAG_05: '05',
      // TAG_06: '06',
      // TAG_07: '07',
      // TAG_08: '08',
      // TAG_09: '09',
      // TAG_10: '10',
      // TAG_11: '11',
      // TAG_12: '12',
      // TAG_13: '13',
      // TAG_14: '14',
      // TAG_15: '15',
      // TAG_16: '16',
      // TAG_17: '17',
      // TAG_18: '18',
      // TAG_19: '19',
      // TAG_20: '20',
      // TAG_21: '21',
      // TAG_22: '22',
      // TAG_23: '23',
      // TAG_24: '24',
      // TAG_25: '25',
      // TAG_26: '26',
      // TAG_27: '27',
      // TAG_28: '28',
      // TAG_29: '29',
      // TAG_30: '30',
      // TAG_31: '31',
      // TAG_32: '32',
      // TAG_33: '33',
      // TAG_34: '34',
      // TAG_35: '35',
      // TAG_36: '36',
      // TAG_37: '37',
      // TAG_38: '38',
      // TAG_39: '39',
      // TAG_40: '40',
      // TAG_41: '41',
      // TAG_42: '42',
      // TAG_43: '43',
      // TAG_44: '44',
      // TAG_45: '45',
      // TAG_46: '46',
      // TAG_47: '47',
      // TAG_48: '48',
      // TAG_49: '49',
      // TAG_50: '50',
      // TAG_51: '51',
    });
  }
});

// node_modules/crypt/crypt.js
var require_crypt = __commonJS({
  "node_modules/crypt/crypt.js"(exports, module) {
    (function() {
      var base64map = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/", crypt = {
        // Bit-wise rotation left
        rotl: function(n, b) {
          return n << b | n >>> 32 - b;
        },
        // Bit-wise rotation right
        rotr: function(n, b) {
          return n << 32 - b | n >>> b;
        },
        // Swap big-endian to little-endian and vice versa
        endian: function(n) {
          if (n.constructor == Number) {
            return crypt.rotl(n, 8) & 16711935 | crypt.rotl(n, 24) & 4278255360;
          }
          for (var i = 0; i < n.length; i++)
            n[i] = crypt.endian(n[i]);
          return n;
        },
        // Generate an array of any length of random bytes
        randomBytes: function(n) {
          for (var bytes = []; n > 0; n--)
            bytes.push(Math.floor(Math.random() * 256));
          return bytes;
        },
        // Convert a byte array to big-endian 32-bit words
        bytesToWords: function(bytes) {
          for (var words = [], i = 0, b = 0; i < bytes.length; i++, b += 8)
            words[b >>> 5] |= bytes[i] << 24 - b % 32;
          return words;
        },
        // Convert big-endian 32-bit words to a byte array
        wordsToBytes: function(words) {
          for (var bytes = [], b = 0; b < words.length * 32; b += 8)
            bytes.push(words[b >>> 5] >>> 24 - b % 32 & 255);
          return bytes;
        },
        // Convert a byte array to a hex string
        bytesToHex: function(bytes) {
          for (var hex = [], i = 0; i < bytes.length; i++) {
            hex.push((bytes[i] >>> 4).toString(16));
            hex.push((bytes[i] & 15).toString(16));
          }
          return hex.join("");
        },
        // Convert a hex string to a byte array
        hexToBytes: function(hex) {
          for (var bytes = [], c = 0; c < hex.length; c += 2)
            bytes.push(parseInt(hex.substr(c, 2), 16));
          return bytes;
        },
        // Convert a byte array to a base-64 string
        bytesToBase64: function(bytes) {
          for (var base64 = [], i = 0; i < bytes.length; i += 3) {
            var triplet = bytes[i] << 16 | bytes[i + 1] << 8 | bytes[i + 2];
            for (var j = 0; j < 4; j++)
              if (i * 8 + j * 6 <= bytes.length * 8)
                base64.push(base64map.charAt(triplet >>> 6 * (3 - j) & 63));
              else
                base64.push("=");
          }
          return base64.join("");
        },
        // Convert a base-64 string to a byte array
        base64ToBytes: function(base64) {
          base64 = base64.replace(/[^A-Z0-9+\/]/ig, "");
          for (var bytes = [], i = 0, imod4 = 0; i < base64.length; imod4 = ++i % 4) {
            if (imod4 == 0) continue;
            bytes.push((base64map.indexOf(base64.charAt(i - 1)) & Math.pow(2, -2 * imod4 + 8) - 1) << imod4 * 2 | base64map.indexOf(base64.charAt(i)) >>> 6 - imod4 * 2);
          }
          return bytes;
        }
      };
      module.exports = crypt;
    })();
  }
});

// node_modules/charenc/charenc.js
var require_charenc = __commonJS({
  "node_modules/charenc/charenc.js"(exports, module) {
    var charenc = {
      // UTF-8 encoding
      utf8: {
        // Convert a string to a byte array
        stringToBytes: function(str) {
          return charenc.bin.stringToBytes(unescape(encodeURIComponent(str)));
        },
        // Convert a byte array to a string
        bytesToString: function(bytes) {
          return decodeURIComponent(escape(charenc.bin.bytesToString(bytes)));
        }
      },
      // Binary encoding
      bin: {
        // Convert a string to a byte array
        stringToBytes: function(str) {
          for (var bytes = [], i = 0; i < str.length; i++)
            bytes.push(str.charCodeAt(i) & 255);
          return bytes;
        },
        // Convert a byte array to a string
        bytesToString: function(bytes) {
          for (var str = [], i = 0; i < bytes.length; i++)
            str.push(String.fromCharCode(bytes[i]));
          return str.join("");
        }
      }
    };
    module.exports = charenc;
  }
});

// node_modules/is-buffer/index.js
var require_is_buffer = __commonJS({
  "node_modules/is-buffer/index.js"(exports, module) {
    module.exports = function(obj) {
      return obj != null && (isBuffer(obj) || isSlowBuffer(obj) || !!obj._isBuffer);
    };
    function isBuffer(obj) {
      return !!obj.constructor && typeof obj.constructor.isBuffer === "function" && obj.constructor.isBuffer(obj);
    }
    function isSlowBuffer(obj) {
      return typeof obj.readFloatLE === "function" && typeof obj.slice === "function" && isBuffer(obj.slice(0, 0));
    }
  }
});

// node_modules/md5/md5.js
var require_md5 = __commonJS({
  "node_modules/md5/md5.js"(exports, module) {
    (function() {
      var crypt = require_crypt(), utf8 = require_charenc().utf8, isBuffer = require_is_buffer(), bin = require_charenc().bin, md5 = function(message, options) {
        if (message.constructor == String)
          if (options && options.encoding === "binary")
            message = bin.stringToBytes(message);
          else
            message = utf8.stringToBytes(message);
        else if (isBuffer(message))
          message = Array.prototype.slice.call(message, 0);
        else if (!Array.isArray(message) && message.constructor !== Uint8Array)
          message = message.toString();
        var m = crypt.bytesToWords(message), l = message.length * 8, a = 1732584193, b = -271733879, c = -1732584194, d = 271733878;
        for (var i = 0; i < m.length; i++) {
          m[i] = (m[i] << 8 | m[i] >>> 24) & 16711935 | (m[i] << 24 | m[i] >>> 8) & 4278255360;
        }
        m[l >>> 5] |= 128 << l % 32;
        m[(l + 64 >>> 9 << 4) + 14] = l;
        var FF = md5._ff, GG = md5._gg, HH = md5._hh, II = md5._ii;
        for (var i = 0; i < m.length; i += 16) {
          var aa = a, bb = b, cc = c, dd = d;
          a = FF(a, b, c, d, m[i + 0], 7, -680876936);
          d = FF(d, a, b, c, m[i + 1], 12, -389564586);
          c = FF(c, d, a, b, m[i + 2], 17, 606105819);
          b = FF(b, c, d, a, m[i + 3], 22, -1044525330);
          a = FF(a, b, c, d, m[i + 4], 7, -176418897);
          d = FF(d, a, b, c, m[i + 5], 12, 1200080426);
          c = FF(c, d, a, b, m[i + 6], 17, -1473231341);
          b = FF(b, c, d, a, m[i + 7], 22, -45705983);
          a = FF(a, b, c, d, m[i + 8], 7, 1770035416);
          d = FF(d, a, b, c, m[i + 9], 12, -1958414417);
          c = FF(c, d, a, b, m[i + 10], 17, -42063);
          b = FF(b, c, d, a, m[i + 11], 22, -1990404162);
          a = FF(a, b, c, d, m[i + 12], 7, 1804603682);
          d = FF(d, a, b, c, m[i + 13], 12, -40341101);
          c = FF(c, d, a, b, m[i + 14], 17, -1502002290);
          b = FF(b, c, d, a, m[i + 15], 22, 1236535329);
          a = GG(a, b, c, d, m[i + 1], 5, -165796510);
          d = GG(d, a, b, c, m[i + 6], 9, -1069501632);
          c = GG(c, d, a, b, m[i + 11], 14, 643717713);
          b = GG(b, c, d, a, m[i + 0], 20, -373897302);
          a = GG(a, b, c, d, m[i + 5], 5, -701558691);
          d = GG(d, a, b, c, m[i + 10], 9, 38016083);
          c = GG(c, d, a, b, m[i + 15], 14, -660478335);
          b = GG(b, c, d, a, m[i + 4], 20, -405537848);
          a = GG(a, b, c, d, m[i + 9], 5, 568446438);
          d = GG(d, a, b, c, m[i + 14], 9, -1019803690);
          c = GG(c, d, a, b, m[i + 3], 14, -187363961);
          b = GG(b, c, d, a, m[i + 8], 20, 1163531501);
          a = GG(a, b, c, d, m[i + 13], 5, -1444681467);
          d = GG(d, a, b, c, m[i + 2], 9, -51403784);
          c = GG(c, d, a, b, m[i + 7], 14, 1735328473);
          b = GG(b, c, d, a, m[i + 12], 20, -1926607734);
          a = HH(a, b, c, d, m[i + 5], 4, -378558);
          d = HH(d, a, b, c, m[i + 8], 11, -2022574463);
          c = HH(c, d, a, b, m[i + 11], 16, 1839030562);
          b = HH(b, c, d, a, m[i + 14], 23, -35309556);
          a = HH(a, b, c, d, m[i + 1], 4, -1530992060);
          d = HH(d, a, b, c, m[i + 4], 11, 1272893353);
          c = HH(c, d, a, b, m[i + 7], 16, -155497632);
          b = HH(b, c, d, a, m[i + 10], 23, -1094730640);
          a = HH(a, b, c, d, m[i + 13], 4, 681279174);
          d = HH(d, a, b, c, m[i + 0], 11, -358537222);
          c = HH(c, d, a, b, m[i + 3], 16, -722521979);
          b = HH(b, c, d, a, m[i + 6], 23, 76029189);
          a = HH(a, b, c, d, m[i + 9], 4, -640364487);
          d = HH(d, a, b, c, m[i + 12], 11, -421815835);
          c = HH(c, d, a, b, m[i + 15], 16, 530742520);
          b = HH(b, c, d, a, m[i + 2], 23, -995338651);
          a = II(a, b, c, d, m[i + 0], 6, -198630844);
          d = II(d, a, b, c, m[i + 7], 10, 1126891415);
          c = II(c, d, a, b, m[i + 14], 15, -1416354905);
          b = II(b, c, d, a, m[i + 5], 21, -57434055);
          a = II(a, b, c, d, m[i + 12], 6, 1700485571);
          d = II(d, a, b, c, m[i + 3], 10, -1894986606);
          c = II(c, d, a, b, m[i + 10], 15, -1051523);
          b = II(b, c, d, a, m[i + 1], 21, -2054922799);
          a = II(a, b, c, d, m[i + 8], 6, 1873313359);
          d = II(d, a, b, c, m[i + 15], 10, -30611744);
          c = II(c, d, a, b, m[i + 6], 15, -1560198380);
          b = II(b, c, d, a, m[i + 13], 21, 1309151649);
          a = II(a, b, c, d, m[i + 4], 6, -145523070);
          d = II(d, a, b, c, m[i + 11], 10, -1120210379);
          c = II(c, d, a, b, m[i + 2], 15, 718787259);
          b = II(b, c, d, a, m[i + 9], 21, -343485551);
          a = a + aa >>> 0;
          b = b + bb >>> 0;
          c = c + cc >>> 0;
          d = d + dd >>> 0;
        }
        return crypt.endian([a, b, c, d]);
      };
      md5._ff = function(a, b, c, d, x, s, t) {
        var n = a + (b & c | ~b & d) + (x >>> 0) + t;
        return (n << s | n >>> 32 - s) + b;
      };
      md5._gg = function(a, b, c, d, x, s, t) {
        var n = a + (b & d | c & ~d) + (x >>> 0) + t;
        return (n << s | n >>> 32 - s) + b;
      };
      md5._hh = function(a, b, c, d, x, s, t) {
        var n = a + (b ^ c ^ d) + (x >>> 0) + t;
        return (n << s | n >>> 32 - s) + b;
      };
      md5._ii = function(a, b, c, d, x, s, t) {
        var n = a + (c ^ (b | ~d)) + (x >>> 0) + t;
        return (n << s | n >>> 32 - s) + b;
      };
      md5._blocksize = 16;
      md5._digestsize = 16;
      module.exports = function(message, options) {
        if (message === void 0 || message === null)
          throw new Error("Illegal argument " + message);
        var digestbytes = crypt.wordsToBytes(md5(message, options));
        return options && options.asBytes ? digestbytes : options && options.asString ? bin.bytesToString(digestbytes) : crypt.bytesToHex(digestbytes);
      };
    })();
  }
});

// node_modules/ts-khqr/lib/generate.js
var require_generate = __commonJS({
  "node_modules/ts-khqr/lib/generate.js"(exports) {
    "use strict";
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.generateQR = void 0;
    var constants_1 = require_constants();
    var models_1 = require_models();
    var utils_1 = require_utils();
    var md5_1 = __importDefault(require_md5());
    function generateQR(payload) {
      let merchantInfo;
      if (payload.tag === constants_1.TAG.MERCHANT) {
        merchantInfo = {
          bakongAccountID: payload.accountID,
          merchantID: payload.merchantID,
          acquiringBank: payload.acquiringBank,
          isMerchant: true
        };
      } else {
        merchantInfo = {
          bakongAccountID: payload.accountID,
          accountInformation: payload.merchantID,
          acquiringBank: payload.acquiringBank,
          isMerchant: false
        };
      }
      try {
        const payloadFormatIndicator = new models_1.PayloadFormatIndicator(constants_1.EMV.PAYLOAD_FORMAT_INDICATOR, constants_1.EMV.DEFAULT_PAYLOAD_FORMAT_INDICATOR);
        let QRType = constants_1.EMV.DYNAMIC_QR;
        if (!payload.amount) {
          QRType = constants_1.EMV.STATIC_QR;
        }
        const pointOfInitiationMethod = new models_1.PointOfInitiationMethod(constants_1.EMV.POINT_OF_INITIATION_METHOD, QRType);
        let upi;
        if (payload.upiMerchantAccount) {
          upi = new models_1.UnionpayMerchantAccount(constants_1.EMV.UNIONPAY_MERCHANT_ACCOUNT, payload.upiMerchantAccount);
        }
        const globalUniqueIdentifier = new models_1.GlobalUniqueIdentifier(payload.tag, merchantInfo);
        const { merchantCategoryCode = constants_1.EMV.DEFAULT_MERCHANT_CATEGORY_CODE, countryCode = constants_1.EMV.DEFAULT_COUNTRY_CODE, merchantCity = constants_1.EMV.DEFAULT_MERCHANT_CITY, currency = constants_1.CURRENCY.KHR, additionalData, languageData } = payload;
        const merchantCategory = new models_1.MerchantCategoryCode(constants_1.EMV.MERCHANT_CATEGORY_CODE, merchantCategoryCode);
        const trxnCurrency = new models_1.TransactionCurrency(constants_1.EMV.TRANSACTION_CURRENCY, currency);
        if (currency === constants_1.CURRENCY.USD && upi)
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.UPI_ACCOUNT_INFORMATION_INVALID_CURRENCY);
        const KHQRInstances = [
          payloadFormatIndicator,
          pointOfInitiationMethod,
          ...upi ? [upi] : [],
          globalUniqueIdentifier,
          merchantCategory,
          trxnCurrency
        ];
        if (payload.amount) {
          let amountInput = String(payload.amount);
          if (currency === constants_1.CURRENCY.KHR) {
            if (payload.amount % 1 === 0) {
              amountInput = String(Math.round(payload.amount));
            } else {
              throw (0, utils_1.response)(null, constants_1.ERROR_CODE.TRANSACTION_AMOUNT_INVALID);
            }
          } else {
            const amountSplit = String(payload.amount).split(".");
            const precision = amountSplit[1];
            if (precision !== void 0 && precision.length > 2) {
              throw (0, utils_1.response)(null, constants_1.ERROR_CODE.TRANSACTION_AMOUNT_INVALID);
            }
            if (precision !== void 0) {
              amountInput = parseFloat(amountInput).toFixed(2);
            }
          }
          KHQRInstances.push(new models_1.TransactionAmount(constants_1.EMV.TRANSACTION_AMOUNT, amountInput));
        }
        KHQRInstances.push(new models_1.CountryCode(constants_1.EMV.COUNTRY_CODE, countryCode));
        KHQRInstances.push(new models_1.MerchantName(constants_1.EMV.MERCHANT_NAME, payload.merchantName));
        KHQRInstances.push(new models_1.MerchantCity(constants_1.EMV.MERCHANT_CITY, merchantCity));
        if (additionalData) {
          const isEmpty = Object.values(additionalData).every((el) => utils_1.StringUtils.isEmpty(el));
          if (!isEmpty) {
            KHQRInstances.push(new models_1.AdditionalData(constants_1.EMV.ADDITIONAL_DATA_TAG, additionalData));
          }
        }
        if (languageData) {
          const isEmpty = Object.values(languageData).every((el) => utils_1.StringUtils.isEmpty(el));
          if (!isEmpty) {
            KHQRInstances.push(new models_1.MerchantInformationLanguageTemplate(constants_1.EMV.MERCHANT_INFORMATION_LANGUAGE_TEMPLATE, languageData));
          }
        }
        if (QRType === constants_1.EMV.DYNAMIC_QR) {
          if (!payload.expirationTimestamp) {
            throw (0, utils_1.response)(null, constants_1.ERROR_CODE.EXPIRATION_TIMESTAMP_REQUIRED);
          }
          KHQRInstances.push(new models_1.TimeStamp(constants_1.EMV.TIMESTAMP_TAG, {
            creationTimestamp: Date.now(),
            expirationTimestamp: payload.expirationTimestamp
          }, QRType));
        }
        let khqrNoCrc = "";
        KHQRInstances.forEach((item) => {
          khqrNoCrc += item.toString();
        });
        let khqr = khqrNoCrc + constants_1.EMV.CRC + constants_1.EMV.CRC_LENGTH;
        khqr += (0, utils_1.crc16)(khqr);
        return (0, utils_1.response)({
          qr: khqr,
          md5: (0, md5_1.default)(khqr)
        });
      } catch (error) {
        return error;
      }
    }
    exports.generateQR = generateQR;
  }
});

// node_modules/ts-khqr/lib/parser.js
var require_parser = __commonJS({
  "node_modules/ts-khqr/lib/parser.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.parseQR = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var cloneObject = (obj) => JSON.parse(JSON.stringify(obj));
    function parseQR(qrString) {
      const allTags = constants_1.KHQR_TAG.map((item) => item.tag);
      const subTags = constants_1.KHQR_TAG.filter((item) => item.sub === true).map((item) => item.tag);
      const tags = {};
      let merchantType = null;
      let lastTag = "";
      let isMerchantTag = false;
      while (qrString) {
        const cutQRString = utils_1.StringUtils.cutString(qrString);
        const { value, slicedString } = cutQRString;
        let { tag } = cutQRString;
        if (tag === lastTag)
          break;
        const isMerchant = tag === "30";
        if (isMerchant) {
          merchantType = "30";
          tag = "29";
          isMerchantTag = true;
        } else if (tag === "29") {
          merchantType = "29";
        }
        if (allTags.includes(tag)) {
          tags[tag] = value;
        }
        qrString = slicedString;
        lastTag = tag;
      }
      let decodeValue = {
        merchantType
      };
      constants_1.KHQR_SUBTAG.input.map((item) => item.data).forEach((item) => decodeValue = Object.assign(Object.assign({}, decodeValue), item));
      constants_1.KHQR_TAG.forEach((khqrTag) => {
        const { tag } = khqrTag;
        const khqr = constants_1.KHQR_TAG.find((item) => item.tag === tag);
        let value = utils_1.StringUtils.isEmpty(tags[tag]) ? null : tags[tag];
        if (subTags.includes(tag)) {
          const subTagInput = constants_1.KHQR_SUBTAG.input.find((item) => item.tag === tag);
          if (subTagInput) {
            let inputValue = {};
            const inputdata = cloneObject(subTagInput.data);
            while (value) {
              const { tag: subtag, value: subtagValue, slicedString: slicedsubtag } = utils_1.StringUtils.cutString(value);
              const nameSubtag = constants_1.KHQR_SUBTAG.compare.filter((item) => item.tag === tag).find((item) => item.subTag === subtag);
              if (nameSubtag) {
                if (isMerchantTag && nameSubtag.name === "accountInformation") {
                  const merchantID = "merchantID";
                  inputdata[merchantID] = subtagValue;
                } else {
                  inputdata[nameSubtag.name] = subtagValue;
                }
                inputValue = inputdata;
              }
              value = slicedsubtag;
            }
            decodeValue = Object.assign(Object.assign({}, decodeValue), inputValue);
          }
        } else {
          if (khqr) {
            decodeValue[khqr.type] = value;
          }
        }
      });
      return decodeValue;
    }
    exports.parseQR = parseQR;
  }
});

// node_modules/ts-khqr/lib/verify.js
var require_verify = __commonJS({
  "node_modules/ts-khqr/lib/verify.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.verifyQR = void 0;
    var constants_1 = require_constants();
    var utils_1 = require_utils();
    var cloneObject = (obj) => JSON.parse(JSON.stringify(obj));
    function verifyQR(qrString) {
      const allTags = constants_1.KHQR_TAG.map((item) => item.tag);
      const subTags = constants_1.KHQR_TAG.filter((item) => item.sub === true).map((item) => item.tag);
      let requiredTags = constants_1.KHQR_TAG.filter((item) => item.required === true).map((item) => item.tag);
      const tags = [];
      let lastTag = "";
      let isMerchantTag = false;
      while (qrString) {
        const cutQRString = utils_1.StringUtils.cutString(qrString);
        const { value, slicedString } = cutQRString;
        let { tag } = cutQRString;
        if (tag === lastTag)
          break;
        const isMerchant = tag === "30";
        if (isMerchant) {
          tag = "29";
          isMerchantTag = true;
        }
        if (allTags.includes(tag)) {
          tags.push({ tag, value });
          requiredTags = requiredTags.filter((item) => item !== tag);
        }
        qrString = slicedString;
        lastTag = tag;
      }
      if (tags.some((item) => item.tag === "01" && item.value === "12")) {
        if (!tags.some((item) => item.tag === "54")) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.INVALID_DYNAMIC_KHQR);
        }
        if (!tags.some((item) => item.tag === "99")) {
          throw (0, utils_1.response)(null, constants_1.ERROR_CODE.EXPIRATION_TIMESTAMP_REQUIRED);
        }
      }
      if (tags.some((item) => item.tag === "54") && !tags.some((item) => item.tag === "99")) {
        throw (0, utils_1.response)(null, constants_1.ERROR_CODE.EXPIRATION_TIMESTAMP_REQUIRED);
      }
      if (requiredTags.length > 0) {
        const requiredTag = requiredTags[0];
        const missingInstance = constants_1.KHQR_TAG.find((item) => item.tag === requiredTag);
        if (missingInstance) {
          new missingInstance.instance(requiredTag, null);
        }
      }
      let poi;
      tags.forEach((khqrTag) => {
        const { tag } = khqrTag;
        let { value } = khqrTag;
        const khqr = constants_1.KHQR_TAG.find((item) => item.tag === tag);
        let inputValue = value;
        if (khqr) {
          if (tag === constants_1.EMV.POINT_OF_INITIATION_METHOD) {
            poi = value;
          }
          if (subTags.includes(tag)) {
            const subTagInput = constants_1.KHQR_SUBTAG.input.find((item) => item.tag === tag);
            if (subTagInput) {
              const inputdata = cloneObject(subTagInput.data);
              while (value) {
                const { tag: subtag, value: subtagValue, slicedString: slicedsubtag } = utils_1.StringUtils.cutString(value);
                const nameSubtag = constants_1.KHQR_SUBTAG.compare.filter((item) => item.tag === tag).find((item) => item.subTag === subtag);
                if (nameSubtag) {
                  if (isMerchantTag && nameSubtag.name === "accountInformation") {
                    const merchantID = "merchantID";
                    inputdata[merchantID] = subtagValue;
                  } else {
                    inputdata[nameSubtag.name] = subtagValue;
                  }
                  inputValue = inputdata;
                }
                value = slicedsubtag;
              }
              if (tag === constants_1.EMV.TIMESTAMP_TAG) {
                new khqr.instance(tag, inputValue, poi);
              } else {
                new khqr.instance(tag, inputValue);
              }
            }
          } else {
            new khqr.instance(tag, inputValue);
          }
        }
      });
    }
    exports.verifyQR = verifyQR;
  }
});

// node_modules/ts-khqr/lib/index.js
var require_lib = __commonJS({
  "node_modules/ts-khqr/lib/index.js"(exports) {
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.KHQR = exports.COUNTRY = exports.TAG = exports.CURRENCY = void 0;
    var constants_1 = require_constants();
    var generate_1 = require_generate();
    var parser_1 = require_parser();
    var utils_1 = require_utils();
    var verify_1 = require_verify();
    var constants_2 = require_constants();
    Object.defineProperty(exports, "CURRENCY", { enumerable: true, get: function() {
      return constants_2.CURRENCY;
    } });
    Object.defineProperty(exports, "TAG", { enumerable: true, get: function() {
      return constants_2.TAG;
    } });
    Object.defineProperty(exports, "COUNTRY", { enumerable: true, get: function() {
      return constants_2.COUNTRY;
    } });
    var KHQR = class _KHQR {
      static generate(payload) {
        return (0, generate_1.generateQR)(payload);
      }
      static verify(qrString) {
        return { isValid: _KHQR.verifyDetail(qrString).status.code === 0 };
      }
      static verifyDetail(qrString) {
        const crcRegExp = /6304[A-Fa-f0-9]{4}$/;
        if (!crcRegExp.test(qrString)) {
          return (0, utils_1.response)({ isValid: false }, constants_1.ERROR_CODE.KHQR_INVALID);
        }
        const crc = qrString.slice(-4);
        const KHQRNoCrc = qrString.slice(0, -4);
        const validCRC = (0, utils_1.crc16)(KHQRNoCrc) === crc.toUpperCase();
        try {
          if (!validCRC || qrString.length < constants_1.EMV.INVALID_LENGTH.KHQR) {
            throw (0, utils_1.response)(null, constants_1.ERROR_CODE.KHQR_INVALID);
          }
          (0, verify_1.verifyQR)(qrString);
          return (0, utils_1.response)({ isValid: true });
        } catch (error) {
          const status = error === null || error === void 0 ? void 0 : error.status;
          if ((status === null || status === void 0 ? void 0 : status.errorCode) != null && status.message != null) {
            return (0, utils_1.response)({ isValid: false }, { code: status.errorCode, message: status.message });
          }
          return (0, utils_1.response)({ isValid: false }, constants_1.ERROR_CODE.KHQR_INVALID);
        }
      }
      static parse(qrString) {
        if (utils_1.StringUtils.isEmpty(qrString)) {
          return (0, utils_1.response)(null, constants_1.ERROR_CODE.KHQR_INVALID);
        }
        return (0, utils_1.response)((0, parser_1.parseQR)(qrString));
      }
    };
    exports.KHQR = KHQR;
  }
});
export default require_lib();
//# sourceMappingURL=ts-khqr.js.map
